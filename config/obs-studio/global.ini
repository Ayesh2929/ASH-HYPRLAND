; ╔══════════════════════════════════════════════════════════════════════════════╗
; ║  🔥 ASH DOTFILES v5.0 OMEGA — OBS STUDIO GLOBAL CONFIG ULTRA               ║
; ║  🎬 Streaming • Recording • Wayland • PipeWire • Ultra performance          ║
; ╚══════════════════════════════════════════════════════════════════════════════╝

[General]
; ── Theme ─────────────────────────────────────────────
Theme                               = Yami

; ── Language ──────────────────────────────────────────
Language                            = en-US

; ── Update check ──────────────────────────────────────
EnableAutoUpdates                   = false

; ── First run ─────────────────────────────────────────
FirstRun                            = true

; ── Last version ──────────────────────────────────────
LastVersion                         = 0x1E090000

; ── Crash detection ───────────────────────────────────
LastExit                            = Complete

; ── Portable mode ─────────────────────────────────────
PortableMode                        = false

; ── Hotkeys overlay ───────────────────────────────────
HotkeyFocusType                     = NeverDisableHotkeys

; ── Scene collection ──────────────────────────────────
SceneCollectionName                 = ASH Collection

; ── Profile ───────────────────────────────────────────
ProfileName                         = ASH Profile

[BasicWindow]
; ── Window geometry ───────────────────────────────────
geometry                            =

; ── Window state ──────────────────────────────────────
windowState                         =

; ── Dock state ────────────────────────────────────────
DockState                           =

; ── Preview enabled ───────────────────────────────────
PreviewEnabled                      = true

; ── Preview program mode ──────────────────────────────
PreviewProgramMode                  = false

; ── Multiview ─────────────────────────────────────────
MultiviewMouseSwitch                = false
MultiviewDrawNames                  = true
MultiviewDrawSafeAreas              = false
MultiviewLayout                     = 0

; ── Stats open ────────────────────────────────────────
StatsOpen                           = false

; ── Source list ───────────────────────────────────────
SourceListHeight                    = 0

; ── System tray ───────────────────────────────────────
SysTrayEnabled                      = true
SysTrayWhenStarted                  = false
SaveProjectors                      = false
ShowTransitions                     = true
AllowCollectionChangeDuringStream   = false

[Output]
; ── Mode: Simple | Advanced ───────────────────────────
Mode                                = Advanced

[Video]
; ── Canvas (base) resolution ──────────────────────────
BaseCX                              = 1920
BaseCY                              = 1080

; ── Output resolution ─────────────────────────────────
OutputCX                            = 1920
OutputCY                            = 1080

; ── FPS numerator/denominator ─────────────────────────
FPSType                             = 0
FPSCommon                           = 60
FPSInt                              = 60
FPSNum                              = 60
FPSDen                              = 1

; ── Downscale filter: Area | Bicubic | Lanczos | Bilinear
ScaleType                           = Lanczos

; ── Color format ──────────────────────────────────────
ColorFormat                         = NV12

; ── Color space ───────────────────────────────────────
ColorSpace                          = 709

; ── Color range ───────────────────────────────────────
ColorRange                          = Partial

; ── SDR white level ───────────────────────────────────
SdrWhiteLevel                       = 300

; ── HDR nominal peak level ────────────────────────────
HdrNominalPeakLevel                 = 1000

[Audio]
; ── Sample rate ───────────────────────────────────────
SampleRate                          = 48000

; ── Channels: Mono=1, Stereo=2, 2.1=3, 4.0=4, 4.1=5, 5.1=6, 7.1=8
ChannelSetup                        = Stereo

; ── Buffering (ms) ────────────────────────────────────
BufferingTime                       = 20

; ── Monitoring device ─────────────────────────────────
MonitoringDeviceId                  = default
MonitoringDeviceName                = Default

[Hotkeys]
; ── Managed via OBS GUI (stored per scene collection) ─

[Stream1]
; ── Streaming service ─────────────────────────────────
type                                = rtmp_custom

[AdvOut]
; ── Encoder: x264 | nvenc | amd | qsv ────────────────
Encoder                             = obs_x264

; ── Rescale output ────────────────────────────────────
RescaleRes                          =
RescaleFilter                       = 2

; ── Recording path ────────────────────────────────────
RecFilePath                         = ~/Videos/OBS

; ── Recording format ──────────────────────────────────
RecFormat2                          = mkv

; ── Recording encoder ─────────────────────────────────
RecEncoder                          = obs_x264

; ── Recording quality ─────────────────────────────────
RecRescaleRes                       =
RecRescaleFilter                    = 2

; ── Replay buffer ─────────────────────────────────────
RecRBTime                           = 60
RecRBSize                           = 512

; ── Audio bitrate ─────────────────────────────────────
TrackIndex                          = 1
Track1Bitrate                       = 192
Track2Bitrate                       = 192
Track3Bitrate                       = 192
Track4Bitrate                       = 192
Track5Bitrate                       = 192
Track6Bitrate                       = 192
Track1Name                          = Main Audio
Track2Name                          = Game Audio
Track3Name                          = Microphone
Track4Name                          = Desktop Audio
Track5Name                          = Music
Track6Name                          = Commentary

; ── Multi-track streaming ─────────────────────────────
StreamMultiTrackAudio               = false

[SimpleOutput]
; ── Video bitrate (streaming) ─────────────────────────
VBitrate                            = 6000

; ── Audio bitrate ─────────────────────────────────────
ABitrate                            = 192

; ── Streaming encoder ─────────────────────────────────
StreamEncoder                       = x264

; ── Recording quality ─────────────────────────────────
RecQuality                          = Small

; ── Recording encoder ─────────────────────────────────
RecEncoder                          = x264

; ── File path ─────────────────────────────────────────
FilePath                            = ~/Videos/OBS

; ── File format ───────────────────────────────────────
RecFormat                           = mkv

; ── Replay buffer ─────────────────────────────────────
RBTime                              = 60
RBSize                              = 512
RBPrefix                            = Replay

[VideoConf]
; ── Video settings per output ─────────────────────────

[Accessibility]
; ── Color adjustments ─────────────────────────────────
ColorPreset                         = 0

[LogViewer]
; ── Log viewer settings ───────────────────────────────
ShowRecentLog                       = 0
ScrollToBottom                      = 1