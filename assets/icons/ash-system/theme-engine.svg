<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <defs>
    <!-- ═══════════════════════════════════════════════════ -->
    <!-- ASH DOTFILES — THEME ENGINE ICON                   -->
    <!-- Concept: Paint brush + gear/cog + colour palette   -->
    <!-- ═══════════════════════════════════════════════════ -->

    <!-- Background -->
    <radialGradient id="bgGrad" cx="50%" cy="50%" r="50%">
      <stop offset="0%"   stop-color="#1e1e2e"/>
      <stop offset="55%"  stop-color="#181825"/>
      <stop offset="100%" stop-color="#11111b"/>
    </radialGradient>

    <!-- Gear gradient -->
    <linearGradient id="gearGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#cba6f7"/>
      <stop offset="40%"  stop-color="#b4befe"/>
      <stop offset="75%"  stop-color="#89b4fa"/>
      <stop offset="100%" stop-color="#74c7ec"/>
    </linearGradient>

    <linearGradient id="gearFill" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#cba6f7" stop-opacity="0.18"/>
      <stop offset="100%" stop-color="#89b4fa" stop-opacity="0.06"/>
    </linearGradient>

    <!-- Brush gradient -->
    <linearGradient id="brushHandle" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#e8c9a0"/>
      <stop offset="50%"  stop-color="#c4956a"/>
      <stop offset="100%" stop-color="#9e6b44"/>
    </linearGradient>

    <linearGradient id="brushFerule" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#7f849c"/>
      <stop offset="50%"  stop-color="#9399b2"/>
      <stop offset="100%" stop-color="#7f849c"/>
    </linearGradient>

    <linearGradient id="brushTip" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#cba6f7"/>
      <stop offset="50%"  stop-color="#f38ba8"/>
      <stop offset="100%" stop-color="#fab387"/>
    </linearGradient>

    <linearGradient id="brushPaint" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#f9e2af"/>
      <stop offset="50%"  stop-color="#fab387"/>
      <stop offset="100%" stop-color="#f38ba8"/>
    </linearGradient>

    <!-- Palette colours -->
    <linearGradient id="paletteBase" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#313244"/>
      <stop offset="100%" stop-color="#181825"/>
    </linearGradient>

    <!-- Paint stroke gradients -->
    <linearGradient id="stroke1" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#cba6f7" stop-opacity="0.9"/>
      <stop offset="100%" stop-color="#cba6f7" stop-opacity="0.2"/>
    </linearGradient>
    <linearGradient id="stroke2" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#89b4fa" stop-opacity="0.9"/>
      <stop offset="100%" stop-color="#89b4fa" stop-opacity="0.2"/>
    </linearGradient>
    <linearGradient id="stroke3" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#a6e3a1" stop-opacity="0.9"/>
      <stop offset="100%" stop-color="#a6e3a1" stop-opacity="0.2"/>
    </linearGradient>
    <linearGradient id="stroke4" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#f9e2af" stop-opacity="0.9"/>
      <stop offset="100%" stop-color="#f9e2af" stop-opacity="0.2"/>
    </linearGradient>
    <linearGradient id="stroke5" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#f38ba8" stop-opacity="0.9"/>
      <stop offset="100%" stop-color="#f38ba8" stop-opacity="0.2"/>
    </linearGradient>

    <!-- Ripple gradient -->
    <radialGradient id="rippleGrad" cx="50%" cy="50%" r="50%">
      <stop offset="0%"   stop-color="#cba6f7" stop-opacity="0.3"/>
      <stop offset="60%"  stop-color="#89b4fa" stop-opacity="0.12"/>
      <stop offset="100%" stop-color="#cba6f7" stop-opacity="0"/>
    </radialGradient>

    <!-- Ring gradient -->
    <linearGradient id="ringGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#cba6f7" stop-opacity="0.5"/>
      <stop offset="50%"  stop-color="#89b4fa" stop-opacity="0.3"/>
      <stop offset="100%" stop-color="#a6e3a1" stop-opacity="0.5"/>
    </linearGradient>

    <!-- Lightning gradient -->
    <linearGradient id="boltGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#f9e2af"/>
      <stop offset="100%" stop-color="#fab387"/>
    </linearGradient>

    <!-- Shimmer -->
    <linearGradient id="shimmer" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#ffffff" stop-opacity="0"/>
      <stop offset="50%"  stop-color="#ffffff" stop-opacity="0.10"/>
      <stop offset="100%" stop-color="#ffffff" stop-opacity="0"/>
    </linearGradient>

    <!-- Glow filters -->
    <filter id="glowGear" x="-25%" y="-25%" width="150%" height="150%">
      <feGaussianBlur stdDeviation="8" result="b1"/>
      <feGaussianBlur stdDeviation="3" result="b2"/>
      <feMerge><feMergeNode in="b1"/><feMergeNode in="b2"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <filter id="glowBrush" x="-25%" y="-25%" width="150%" height="150%">
      <feGaussianBlur stdDeviation="6" result="b"/>
      <feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <filter id="glowColour" x="-30%" y="-30%" width="160%" height="160%">
      <feGaussianBlur stdDeviation="5" result="b"/>
      <feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <filter id="glowSoft" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur stdDeviation="4" result="b"/>
      <feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <filter id="shadowDeep" x="-15%" y="-15%" width="130%" height="130%">
      <feDropShadow dx="0" dy="6" stdDeviation="16" flood-color="#000" flood-opacity="0.55"/>
    </filter>

    <filter id="glowOuter" x="-40%" y="-40%" width="180%" height="180%">
      <feGaussianBlur stdDeviation="16" result="b"/>
      <feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <!-- Clip circle -->
    <clipPath id="circleClip">
      <circle cx="256" cy="256" r="242"/>
    </clipPath>

    <!-- Gear tooth path (computed for 12-tooth gear) -->
    <!-- Gear is built procedurally using transform rotations -->
  </defs>

  <!-- ══════════════════════════════════════════════════════ -->
  <!-- LAYER 0: Background circle                            -->
  <!-- ══════════════════════════════════════════════════════ -->
  <circle cx="256" cy="256" r="252" fill="url(#bgGrad)"/>

  <!-- outer ring -->
  <circle cx="256" cy="256" r="248"
          fill="none"
          stroke="url(#ringGrad)"
          stroke-width="1"
          opacity="0.35"
          stroke-dasharray="8 6"/>

  <!-- inner ring -->
  <circle cx="256" cy="256" r="234"
          fill="none"
          stroke="#cba6f7"
          stroke-width="0.6"
          opacity="0.14"/>

  <!-- ambient glow -->
  <circle cx="256" cy="256" r="200" fill="url(#rippleGrad)"/>

  <!-- ══════════════════════════════════════════════════════ -->
  <!-- LAYER 1: Paint strokes background (before gear)       -->
  <!-- ══════════════════════════════════════════════════════ -->

  <!-- paint strokes — horizontal bands in upper area -->
  <g clip-path="url(#circleClip)" opacity="0.45">
    <!-- stroke purple -->
    <rect x="60" y="108" width="200" height="14" rx="7"
          fill="url(#stroke1)" transform="rotate(-8,160,115)"/>
    <!-- stroke blue -->
    <rect x="50" y="132" width="180" height="12" rx="6"
          fill="url(#stroke2)" transform="rotate(-8,140,138)"/>
    <!-- stroke green -->
    <rect x="55" y="154" width="160" height="10" rx="5"
          fill="url(#stroke3)" transform="rotate(-6,135,159)"/>
    <!-- stroke yellow -->
    <rect x="58" y="174" width="140" height="10" rx="5"
          fill="url(#stroke4)" transform="rotate(-6,128,179)"/>
    <!-- stroke red -->
    <rect x="60" y="193" width="120" height="9" rx="4.5"
          fill="url(#stroke5)" transform="rotate(-5,120,197)"/>
  </g>

  <!-- ══════════════════════════════════════════════════════ -->
  <!-- LAYER 2: GEAR / COG (main engine symbol)             -->
  <!-- ══════════════════════════════════════════════════════ -->

  <!-- Gear construction:
       - Outer radius: 148px
       - Inner radius: 108px
       - Hub radius:   42px
       - 12 teeth, each tooth is a small rectangle -->

  <g transform="translate(256,256)">

    <!-- gear outer glow bloom -->
    <circle r="155" fill="none" stroke="#cba6f7" stroke-width="8" opacity="0.06"
            filter="url(#glowOuter)"/>

    <!-- gear body fill -->
    <circle r="145" fill="url(#gearFill)"/>

    <!-- 12 teeth — each tooth is a rounded rect rotated around centre -->
    <g fill="url(#gearGrad)" filter="url(#glowGear)" opacity="0.92">
      <!-- tooth 0 -->
      <rect x="-16" y="-160" width="32" height="32" rx="5"
            transform="rotate(0)"/>
      <!-- tooth 1 -->
      <rect x="-16" y="-160" width="32" height="32" rx="5"
            transform="rotate(30)"/>
      <!-- tooth 2 -->
      <rect x="-16" y="-160" width="32" height="32" rx="5"
            transform="rotate(60)"/>
      <!-- tooth 3 -->
      <rect x="-16" y="-160" width="32" height="32" rx="5"
            transform="rotate(90)"/>
      <!-- tooth 4 -->
      <rect x="-16" y="-160" width="32" height="32" rx="5"
            transform="rotate(120)"/>
      <!-- tooth 5 -->
      <rect x="-16" y="-160" width="32" height="32" rx="5"
            transform="rotate(150)"/>
      <!-- tooth 6 -->
      <rect x="-16" y="-160" width="32" height="32" rx="5"
            transform="rotate(180)"/>
      <!-- tooth 7 -->
      <rect x="-16" y="-160" width="32" height="32" rx="5"
            transform="rotate(210)"/>
      <!-- tooth 8 -->
      <rect x="-16" y="-160" width="32" height="32" rx="5"
            transform="rotate(240)"/>
      <!-- tooth 9 -->
      <rect x="-16" y="-160" width="32" height="32" rx="5"
            transform="rotate(270)"/>
      <!-- tooth 10 -->
      <rect x="-16" y="-160" width="32" height="32" rx="5"
            transform="rotate(300)"/>
      <!-- tooth 11 -->
      <rect x="-16" y="-160" width="32" height="32" rx="5"
            transform="rotate(330)"/>
    </g>

    <!-- gear main disk -->
    <circle r="140"
            fill="url(#bgGrad)"
            stroke="url(#gearGrad)"
            stroke-width="3"
            filter="url(#glowGear)"/>

    <!-- gear inner border ring -->
    <circle r="136"
            fill="none"
            stroke="#cba6f7"
            stroke-width="0.8"
            opacity="0.30"/>

    <!-- inner ring detail circle -->
    <circle r="110"
            fill="none"
            stroke="url(#gearGrad)"
            stroke-width="1.5"
            opacity="0.40"
            stroke-dasharray="5 7"/>

    <!-- gear spokes — 6 spokes -->
    <g stroke="url(#gearGrad)" stroke-width="4" stroke-linecap="round" opacity="0.35">
      <line x1="0" y1="-100" x2="0"   y2="-44"/>
      <line x1="0" y1="-100" x2="0"   y2="-44" transform="rotate(60)"/>
      <line x1="0" y1="-100" x2="0"   y2="-44" transform="rotate(120)"/>
      <line x1="0" y1="-100" x2="0"   y2="-44" transform="rotate(180)"/>
      <line x1="0" y1="-100" x2="0"   y2="-44" transform="rotate(240)"/>
      <line x1="0" y1="-100" x2="0"   y2="-44" transform="rotate(300)"/>
    </g>

    <!-- inner spoke ring -->
    <circle r="44" fill="url(#gearFill)" stroke="url(#gearGrad)" stroke-width="2" opacity="0.6"/>

    <!-- hub -->
    <circle r="30" fill="url(#bgGrad)" stroke="url(#gearGrad)" stroke-width="2.5"
            filter="url(#glowGear)"/>

    <!-- hub shimmer -->
    <circle r="30" fill="url(#shimmer)" opacity="0.6"/>

    <!-- hub inner ring -->
    <circle r="24" fill="none" stroke="#cba6f7" stroke-width="1" opacity="0.4"
            stroke-dasharray="3 5"/>

    <!-- hub dot -->
    <circle r="8" fill="url(#gearGrad)" filter="url(#glowSoft)"/>
    <circle r="4" fill="#ffffff" opacity="0.35"/>

    <!-- shimmer overlay on gear body -->
    <circle r="140" fill="url(#shimmer)" opacity="0.5"/>

  </g>

  <!-- ══════════════════════════════════════════════════════ -->
  <!-- LAYER 3: PAINT BRUSH (overlaid, tilted)               -->
  <!-- ══════════════════════════════════════════════════════ -->

  <!-- Brush: tilted 45deg, positioned top-left to bottom-right -->
  <g transform="translate(256,256) rotate(-40) translate(0,-30)" filter="url(#shadowDeep)">

    <!-- ── brush handle ── -->
    <!-- main handle body -->
    <rect x="-8" y="-170" width="16" height="170" rx="8"
          fill="url(#brushHandle)"/>

    <!-- handle highlight stripe -->
    <rect x="-5" y="-165" width="6" height="155" rx="3"
          fill="#ffffff" opacity="0.12"/>

    <!-- handle top cap -->
    <ellipse cx="0" cy="-170" rx="8" ry="5"
             fill="#b8906a"/>

    <!-- handle grip bands -->
    <g fill="#9e6b44" opacity="0.4">
      <rect x="-8" y="-120" width="16" height="4" rx="2"/>
      <rect x="-8" y="-108" width="16" height="4" rx="2"/>
      <rect x="-8" y="-96"  width="16" height="4" rx="2"/>
    </g>

    <!-- ── ferrule (metal band) ── -->
    <rect x="-10" y="-8" width="20" height="32" rx="3"
          fill="url(#brushFerule)"/>
    <!-- ferrule highlight -->
    <rect x="-8" y="-6" width="6" height="28" rx="2"
          fill="#ffffff" opacity="0.14"/>
    <!-- ferrule accent lines -->
    <g stroke="#585b70" stroke-width="0.8" opacity="0.5">
      <line x1="-10" y1="0"  x2="10"  y2="0"/>
      <line x1="-10" y1="6"  x2="10"  y2="6"/>
      <line x1="-10" y1="12" x2="10"  y2="12"/>
      <line x1="-10" y1="18" x2="10"  y2="18"/>
    </g>

    <!-- ── bristles ── -->
    <!-- bristle body — tapered shape -->
    <path d="M-10,24 L-12,64 Q0,72 12,64 L10,24 Z"
          fill="url(#brushTip)"/>

    <!-- individual bristle strands -->
    <g opacity="0.7">
      <line x1="-8"  y1="24" x2="-10" y2="68" stroke="#cba6f7" stroke-width="1.5" stroke-linecap="round"/>
      <line x1="-4"  y1="24" x2="-5"  y2="70" stroke="#e8729a" stroke-width="1.5" stroke-linecap="round"/>
      <line x1="0"   y1="24" x2="0"   y2="72" stroke="#f38ba8" stroke-width="1.5" stroke-linecap="round"/>
      <line x1="4"   y1="24" x2="5"   y2="70" stroke="#fab387" stroke-width="1.5" stroke-linecap="round"/>
      <line x1="8"   y1="24" x2="10"  y2="68" stroke="#cba6f7" stroke-width="1.5" stroke-linecap="round"/>
    </g>

    <!-- bristle tip highlight -->
    <ellipse cx="0" cy="66" rx="6" ry="3"
             fill="#ffffff" opacity="0.15"/>

    <!-- ── paint blob at tip ── -->
    <ellipse cx="0" cy="74" rx="12" ry="8"
             fill="url(#brushPaint)"
             filter="url(#glowBrush)"/>

    <!-- paint drip -->
    <path d="M-3,80 Q-4,96 -2,104 Q0,108 2,104 Q4,96 3,80 Z"
          fill="#fab387" opacity="0.7"/>

    <!-- paint drip tip glow -->
    <circle cx="0" cy="105" r="3" fill="#f9e2af" opacity="0.6"
            filter="url(#glowSoft)"/>

  </g>

  <!-- ══════════════════════════════════════════════════════ -->
  <!-- LAYER 4: COLOUR PALETTE (bottom-right arc)            -->
  <!-- ══════════════════════════════════════════════════════ -->

  <!-- palette body — kidney/arc shape -->
  <g transform="translate(256,256)">

    <!-- palette board -->
    <ellipse cx="60" cy="70" rx="70" ry="55"
             fill="url(#paletteBase)"
             stroke="#45475a"
             stroke-width="1.5"
             filter="url(#shadowDeep)"
             transform="rotate(-15,60,70)"/>

    <!-- palette shimmer -->
    <ellipse cx="60" cy="70" rx="70" ry="55"
             fill="url(#shimmer)"
             opacity="0.5"
             transform="rotate(-15,60,70)"/>

    <!-- thumb hole -->
    <ellipse cx="38" cy="90" rx="12" ry="10"
             fill="#181825"
             transform="rotate(-15,38,90)"/>

    <!-- colour swatches on palette -->
    <g filter="url(#glowColour)">
      <!-- swatch 1: purple -->
      <circle cx="52"  cy="52"  r="11" fill="#cba6f7"/>
      <circle cx="52"  cy="52"  r="8"  fill="#cba6f7" opacity="0.8"/>
      <circle cx="48"  cy="48"  r="3"  fill="#ffffff" opacity="0.25"/>

      <!-- swatch 2: blue -->
      <circle cx="76"  cy="42"  r="11" fill="#89b4fa"/>
      <circle cx="76"  cy="42"  r="8"  fill="#89b4fa" opacity="0.8"/>
      <circle cx="72"  cy="38"  r="3"  fill="#ffffff" opacity="0.25"/>

      <!-- swatch 3: green -->
      <circle cx="98"  cy="50"  r="11" fill="#a6e3a1"/>
      <circle cx="98"  cy="50"  r="8"  fill="#a6e3a1" opacity="0.8"/>
      <circle cx="94"  cy="46"  r="3"  fill="#ffffff" opacity="0.25"/>

      <!-- swatch 4: yellow -->
      <circle cx="108" cy="72"  r="10" fill="#f9e2af"/>
      <circle cx="108" cy="72"  r="7"  fill="#f9e2af" opacity="0.8"/>
      <circle cx="104" cy="68"  r="2.5" fill="#ffffff" opacity="0.25"/>

      <!-- swatch 5: red/pink -->
      <circle cx="96"  cy="92"  r="10" fill="#f38ba8"/>
      <circle cx="96"  cy="92"  r="7"  fill="#f38ba8" opacity="0.8"/>
      <circle cx="92"  cy="88"  r="2.5" fill="#ffffff" opacity="0.25"/>

      <!-- swatch 6: peach -->
      <circle cx="74"  cy="100" r="10" fill="#fab387"/>
      <circle cx="74"  cy="100" r="7"  fill="#fab387" opacity="0.8"/>
      <circle cx="70"  cy="96"  r="2.5" fill="#ffffff" opacity="0.25"/>

      <!-- swatch 7: teal -->
      <circle cx="54"  cy="94"  r="9" fill="#94e2d5"/>
      <circle cx="54"  cy="94"  r="6"  fill="#94e2d5" opacity="0.8"/>
      <circle cx="50"  cy="90"  r="2.5" fill="#ffffff" opacity="0.25"/>
    </g>

    <!-- palette border glow -->
    <ellipse cx="60" cy="70" rx="70" ry="55"
             fill="none"
             stroke="url(#gearGrad)"
             stroke-width="1.5"
             opacity="0.4"
             transform="rotate(-15,60,70)"
             filter="url(#glowSoft)"/>

  </g>

  <!-- ══════════════════════════════════════════════════════ -->
  <!-- LAYER 5: Lightning bolt accent (speed indicator)      -->
  <!-- ══════════════════════════════════════════════════════ -->

  <!-- bolt — top left accent -->
  <g transform="translate(106,106)" filter="url(#glowColour)">
    <!-- glow bloom -->
    <path d="M22,0 L8,22 L18,22 L6,44 L26,16 L14,16 Z"
          fill="#f9e2af" opacity="0.2"
          filter="url(#glowBrush)"/>
    <!-- bolt body -->
    <path d="M22,0 L8,22 L18,22 L6,44 L26,16 L14,16 Z"
          fill="url(#boltGrad)"/>
    <!-- bolt highlight -->
    <path d="M19,3 L10,20 L17,20 L9,40 L22,18 L14,18 Z"
          fill="#ffffff" opacity="0.20"/>
  </g>

  <!-- ══════════════════════════════════════════════════════ -->
  <!-- LAYER 6: Floating colour particle specks              -->
  <!-- ══════════════════════════════════════════════════════ -->

  <!-- scattered colour dots suggesting theme diversity -->
  <g opacity="0.55" filter="url(#glowColour)">
    <!-- purple cluster -->
    <circle cx="118" cy="165" r="4.5" fill="#cba6f7"/>
    <circle cx="132" cy="152" r="3"   fill="#cba6f7" opacity="0.7"/>
    <circle cx="148" cy="172" r="3.5" fill="#b4befe"/>

    <!-- blue cluster -->
    <circle cx="374" cy="148" r="4.5" fill="#89b4fa"/>
    <circle cx="360" cy="165" r="3"   fill="#89b4fa" opacity="0.7"/>
    <circle cx="388" cy="160" r="3.5" fill="#74c7ec"/>

    <!-- green speck -->
    <circle cx="142" cy="388" r="4" fill="#a6e3a1"/>
    <circle cx="162" cy="374" r="3" fill="#94e2d5" opacity="0.7"/>

    <!-- peach speck -->
    <circle cx="368" cy="368" r="4" fill="#fab387"/>
    <circle cx="382" cy="350" r="3" fill="#f9e2af" opacity="0.7"/>

    <!-- pink speck -->
    <circle cx="156" cy="315" r="3.5" fill="#f38ba8"/>

    <!-- yellow speck -->
    <circle cx="360" cy="310" r="3.5" fill="#f9e2af"/>
  </g>

  <!-- ══════════════════════════════════════════════════════ -->
  <!-- LAYER 7: Corner bracket decorations                   -->
  <!-- ══════════════════════════════════════════════════════ -->

  <!-- top-left bracket -->
  <g stroke="#cba6f7" stroke-width="2" fill="none" opacity="0.4">
    <path d="M62,96 L62,82 L78,82"/>
  </g>
  <!-- top-right bracket -->
  <g stroke="#b4befe" stroke-width="2" fill="none" opacity="0.4">
    <path d="M450,96 L450,82 L434,82"/>
  </g>
  <!-- bottom-left bracket -->
  <g stroke="#89b4fa" stroke-width="2" fill="none" opacity="0.4">
    <path d="M62,416 L62,430 L78,430"/>
  </g>
  <!-- bottom-right bracket -->
  <g stroke="#74c7ec" stroke-width="2" fill="none" opacity="0.4">
    <path d="M450,416 L450,430 L434,430"/>
  </g>

  <!-- ══════════════════════════════════════════════════════ -->
  <!-- LAYER 8: "TE" text label (theme engine initials)      -->
  <!-- ══════════════════════════════════════════════════════ -->

  <!-- subtle "TE" in gear centre hub area -->
  <g transform="translate(256,256)" opacity="0.0">
    <!-- intentionally invisible — logo should be icon-only -->
    <!-- uncomment if text needed: -->
    <!--
    <text x="0" y="6"
          font-family="JetBrains Mono, monospace"
          font-size="16" font-weight="800"
          fill="url(#gearGrad)"
          text-anchor="middle">TE</text>
    -->
  </g>

  <!-- ══════════════════════════════════════════════════════ -->
  <!-- LAYER 9: Final vignette + outer polish                -->
  <!-- ══════════════════════════════════════════════════════ -->

  <!-- outer glow ring -->
  <circle cx="256" cy="256" r="248"
          fill="none"
          stroke="#cba6f7"
          stroke-width="0.8"
          opacity="0.10"
          filter="url(#glowOuter)"/>

  <!-- vignette dark edge -->
  <radialGradient id="vignette" cx="50%" cy="50%" r="50%">
    <stop offset="58%" stop-color="transparent"/>
    <stop offset="100%" stop-color="#000000" stop-opacity="0.38"/>
  </radialGradient>
  <circle cx="256" cy="256" r="252" fill="url(#vignette)"/>

</svg>