<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN"
  "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<!--
╔══════════════════════════════════════════════════════════════════════════════╗
║  ✅ ASH DOTFILES v5.0 OMEGA — CI/CD STATUS BADGE                            ║
║  Live CI pipeline status with animated build indicator                     ║
║  Dimensions: 150 × 28px  ·  WCAG AA  ·  GitHub-safe                       ║
║                                                                              ║
║  States:                                                                     ║
║    passing  — Catppuccin Green  #A6E3A1  (default — change .ci-state)      ║
║    failing  — Catppuccin Red    #F38BA8  (swap .ci-state fill/stroke)       ║
║    running  — Catppuccin Yellow #F9E2AF  (animated spinner)                ║
║    unknown  — Catppuccin Gray   #6C7086                                     ║
╚══════════════════════════════════════════════════════════════════════════════╝
-->
<svg
  xmlns="http://www.w3.org/2000/svg"
  xmlns:xlink="http://www.w3.org/1999/xlink"
  viewBox="0 0 150 28"
  width="150"
  height="28"
  shape-rendering="geometricPrecision"
  text-rendering="geometricPrecision"
  role="img"
  aria-label="CI: passing">

  <title>CI/CD: passing</title>
  <desc>ASH Dotfiles CI/CD status badge — all pipelines passing</desc>

  <defs>

    <style><![CDATA[

      /* ── Track 01: Label breathe ────────────────────────────────────── */
      .ci-label { animation: ci-breathe 6s ease-in-out infinite; }

      /* ── Track 02: Status text glow ─────────────────────────────────── */
      .ci-status-text { animation: ci-status-glow 3s ease-in-out infinite; }

      /* ── Track 03: Status indicator dot ─────────────────────────────── */
      .ci-dot { animation: ci-dot-heartbeat 2s ease-in-out infinite; transform-origin: 128px 14px; }

      /* ── Track 04: Checkmark draw ───────────────────────────────────── */
      .ci-check { animation: ci-check-draw 0.8s ease-out 0.2s 1 both; }

      /* ── Track 05: Build progress bar ───────────────────────────────── */
      .ci-progress { animation: ci-progress-fill 2s cubic-bezier(0.4,0,0.2,1) 0.1s 1 both; transform-origin: left center; }

      /* ── Track 06-10: Pipeline stage indicators ─────────────────────── */
      .ci-stage-1 { animation: ci-stage-light 8s ease-in-out 0.0s infinite; }
      .ci-stage-2 { animation: ci-stage-light 8s ease-in-out 0.5s infinite; }
      .ci-stage-3 { animation: ci-stage-light 8s ease-in-out 1.0s infinite; }
      .ci-stage-4 { animation: ci-stage-light 8s ease-in-out 1.5s infinite; }
      .ci-stage-5 { animation: ci-stage-light 8s ease-in-out 2.0s infinite; }

      /* ── Track 11-12: Shimmer sweeps ────────────────────────────────── */
      .ci-shim-l { animation: ci-shimmer 5s ease-in-out infinite; }
      .ci-shim-r { animation: ci-shimmer 7s ease-in-out infinite 2.5s; }

      /* ── Track 13: Top accent bar ───────────────────────────────────── */
      .ci-bar { animation: ci-bar-scan 4s ease-in-out infinite; transform-origin: 0 0; }

      /* ── Track 14: Halo ─────────────────────────────────────────────── */
      .ci-halo { animation: ci-halo-pulse 4s ease-in-out infinite; transform-origin: 75px 14px; }

      /* ── Track 15-17: Success particles ─────────────────────────────── */
      .ci-burst-1 { animation: ci-burst 4.0s ease-out 0.4s infinite; }
      .ci-burst-2 { animation: ci-burst 4.8s ease-out 1.6s infinite; }
      .ci-burst-3 { animation: ci-burst 3.6s ease-out 2.8s infinite; }
      .ci-burst-4 { animation: ci-burst 5.2s ease-out 0.9s infinite; }
      .ci-burst-5 { animation: ci-burst 4.4s ease-out 3.5s infinite; }

      /* ── Track 18-20: Build dots (matrix rain effect) ───────────────── */
      .ci-bead-1 { animation: ci-bead-fall 3s linear 0.0s infinite; }
      .ci-bead-2 { animation: ci-bead-fall 3s linear 1.0s infinite; }
      .ci-bead-3 { animation: ci-bead-fall 3s linear 2.0s infinite; }

      /* ── Track 21: Spinning build wheel ─────────────────────────────── */
      .ci-wheel { animation: ci-wheel-spin 8s linear infinite; transform-origin: 11px 14px; }
      .ci-wheel-inner { animation: ci-wheel-spin-r 5s linear infinite; transform-origin: 11px 14px; }

      /* ── Track 22: Tick mark bounce ─────────────────────────────────── */
      .ci-tick { animation: ci-tick-bounce 3s ease-in-out infinite; transform-origin: 135px 14px; }

      /* ── Track 23: Counter number roll ──────────────────────────────── */
      .ci-run-count { animation: ci-counter-tick 6s ease-in-out infinite; }

      /* ── Reduced motion ─────────────────────────────────────────────── */
      @media (prefers-reduced-motion: reduce) {
        * { animation: none !important; transition: none !important; }
        .ci-check { stroke-dashoffset: 0 !important; }
        .ci-progress { transform: scaleX(1) !important; opacity: 1 !important; }
      }

      /* ════════════════════════════════════════════════════════════════════
         KEYFRAMES — CI/CD themed animations
         ════════════════════════════════════════════════════════════════════ */

      @keyframes ci-breathe {
        0%,100% { opacity: 0.88; }
        50%      { opacity: 1;    }
      }

      @keyframes ci-status-glow {
        0%,100% { filter: brightness(1);    }
        50%      { filter: brightness(1.2);  }
      }

      @keyframes ci-dot-heartbeat {
        0%,100% { transform: scale(1);    opacity: 0.9; }
        15%      { transform: scale(1.3);  opacity: 1;   }
        30%      { transform: scale(1);    opacity: 0.85; }
        45%      { transform: scale(1.15); opacity: 0.95; }
        60%      { transform: scale(1);    opacity: 0.9; }
      }

      @keyframes ci-check-draw {
        from { stroke-dashoffset: 20; opacity: 0; }
        to   { stroke-dashoffset: 0;  opacity: 1; }
      }

      @keyframes ci-progress-fill {
        from { transform: scaleX(0); opacity: 0; }
        to   { transform: scaleX(1); opacity: 1; }
      }

      @keyframes ci-stage-light {
        0%,70%,100% { opacity: 0.5; transform: scale(1);    }
        75%          { opacity: 1;   transform: scale(1.25); }
        80%          { opacity: 0.7; transform: scale(0.9);  }
        85%          { opacity: 1;   transform: scale(1.1);  }
      }

      @keyframes ci-shimmer {
        0%   { transform: translateX(-80px) skewX(-7deg); opacity: 0; }
        15%  { opacity: 0.5; }
        85%  { opacity: 0.5; }
        100% { transform: translateX(210px) skewX(-7deg); opacity: 0; }
      }

      @keyframes ci-bar-scan {
        0%,100% { opacity: 0.75; transform: scaleX(1);    }
        50%      { opacity: 1;    transform: scaleX(0.97); }
      }

      @keyframes ci-halo-pulse {
        0%,100% { opacity: 0.3; transform: scale(1);    }
        50%      { opacity: 0.6; transform: scale(1.07); }
      }

      @keyframes ci-burst {
        0%   { transform: translate(0,0) scale(0) rotate(0deg);   opacity: 0; }
        5%   { opacity: 1; }
        50%  { transform: translate(var(--bx,4px),var(--by,-10px)) scale(1.5) rotate(180deg); opacity: 0.9; }
        90%  { opacity: 0.1; }
        100% { transform: translate(var(--bx2,7px),var(--by2,-18px)) scale(0) rotate(360deg); opacity: 0; }
      }

      @keyframes ci-bead-fall {
        0%   { transform: translateY(-4px); opacity: 0; }
        5%   { opacity: 0.8; }
        95%  { opacity: 0.8; }
        100% { transform: translateY(32px);  opacity: 0; }
      }

      @keyframes ci-wheel-spin {
        from { transform: rotate(0deg);   }
        to   { transform: rotate(360deg); }
      }

      @keyframes ci-wheel-spin-r {
        from { transform: rotate(0deg);    }
        to   { transform: rotate(-360deg); }
      }

      @keyframes ci-tick-bounce {
        0%,100% { transform: scale(1)    translateY(0);    }
        33%      { transform: scale(1.2)  translateY(-2px); }
        66%      { transform: scale(0.92) translateY(1px);  }
      }

      @keyframes ci-counter-tick {
        0%,88%,100% { transform: translateY(0);    opacity: 1;   }
        90%          { transform: translateY(-4px); opacity: 0.4; }
        92%          { transform: translateY(3px);  opacity: 0.6; }
        94%          { transform: translateY(0);    opacity: 1;   }
      }

    ]]></style>

    <!-- ════════════════════════════════════════════════════════════════════
         GRADIENT LIBRARY
         ════════════════════════════════════════════════════════════════════ -->

    <!-- Label bg -->
    <linearGradient id="ci-g-label" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%"   stop-color="#1A2010"/>
      <stop offset="100%" stop-color="#1E1E2E"/>
    </linearGradient>

    <!-- Value bg — animated green → teal passing state -->
    <linearGradient id="ci-g-value" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#A6E3A1">
        <animate attributeName="stop-color"
          values="#A6E3A1;#94E2D5;#89DCEB;#A6E3A1"
          dur="9s" repeatCount="indefinite"/>
      </stop>
      <stop offset="50%"  stop-color="#40A02B">
        <animate attributeName="stop-color"
          values="#40A02B;#179299;#94E2D5;#40A02B"
          dur="9s" repeatCount="indefinite"/>
      </stop>
      <stop offset="100%" stop-color="#94E2D5">
        <animate attributeName="stop-color"
          values="#94E2D5;#A6E3A1;#40A02B;#94E2D5"
          dur="9s" repeatCount="indefinite"/>
      </stop>
    </linearGradient>

    <!-- Value dark overlay -->
    <linearGradient id="ci-g-value-dark" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%"   stop-color="#051005" stop-opacity="0.55"/>
      <stop offset="100%" stop-color="#020A02" stop-opacity="0.70"/>
    </linearGradient>

    <!-- Border — animated green -->
    <linearGradient id="ci-g-border" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#A6E3A1" stop-opacity="0.65">
        <animate attributeName="stop-color"
          values="#A6E3A1;#94E2D5;#A6E3A1"
          dur="7s" repeatCount="indefinite"/>
      </stop>
      <stop offset="100%" stop-color="#40A02B" stop-opacity="0.32"/>
    </linearGradient>

    <!-- Halo — green radial -->
    <radialGradient id="ci-g-halo" cx="50%" cy="50%" r="50%">
      <stop offset="0%"   stop-color="#A6E3A1" stop-opacity="0.4">
        <animate attributeName="stop-color"
          values="#A6E3A1;#94E2D5;#89DCEB;#A6E3A1"
          dur="9s" repeatCount="indefinite"/>
      </stop>
      <stop offset="100%" stop-color="#A6E3A1" stop-opacity="0"/>
    </radialGradient>

    <!-- Glass -->
    <linearGradient id="ci-g-glass" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%"   stop-color="#FFFFFF" stop-opacity="0.14"/>
      <stop offset="55%"  stop-color="#FFFFFF" stop-opacity="0.03"/>
      <stop offset="100%" stop-color="#FFFFFF" stop-opacity="0"/>
    </linearGradient>

    <!-- Shimmer -->
    <linearGradient id="ci-g-shimmer" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#FFFFFF" stop-opacity="0"/>
      <stop offset="50%"  stop-color="#FFFFFF" stop-opacity="0.48"/>
      <stop offset="100%" stop-color="#FFFFFF" stop-opacity="0"/>
    </linearGradient>

    <!-- Progress bar gradient -->
    <linearGradient id="ci-g-progress" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#A6E3A1" stop-opacity="0.6"/>
      <stop offset="60%"  stop-color="#94E2D5" stop-opacity="0.7"/>
      <stop offset="100%" stop-color="#89DCEB" stop-opacity="0.4"/>
    </linearGradient>

    <!-- Stage indicator gradient — green bright -->
    <radialGradient id="ci-g-stage" cx="50%" cy="50%" r="50%">
      <stop offset="0%"   stop-color="#A6E3A1"/>
      <stop offset="100%" stop-color="#A6E3A1" stop-opacity="0"/>
    </radialGradient>

    <!-- Burst particle fills -->
    <radialGradient id="ci-bf1" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#A6E3A1"/>
      <stop offset="100%" stop-color="#A6E3A1" stop-opacity="0"/>
    </radialGradient>
    <radialGradient id="ci-bf2" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#94E2D5"/>
      <stop offset="100%" stop-color="#94E2D5" stop-opacity="0"/>
    </radialGradient>
    <radialGradient id="ci-bf3" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#89B4FA"/>
      <stop offset="100%" stop-color="#89B4FA" stop-opacity="0"/>
    </radialGradient>
    <radialGradient id="ci-bf4" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#F9E2AF"/>
      <stop offset="100%" stop-color="#F9E2AF" stop-opacity="0"/>
    </radialGradient>
    <radialGradient id="ci-bf5" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#CBA6F7"/>
      <stop offset="100%" stop-color="#CBA6F7" stop-opacity="0"/>
    </radialGradient>

    <!-- ════════════════════════════════════════════════════════════════════
         FILTERS
         ════════════════════════════════════════════════════════════════════ -->

    <filter id="ci-f-shadow" x="-4%" y="-14%" width="108%" height="128%">
      <feDropShadow dx="0" dy="2" stdDeviation="3"
        flood-color="#000000" flood-opacity="0.55"/>
      <feDropShadow dx="0" dy="0" stdDeviation="5"
        flood-color="#A6E3A1" flood-opacity="0.12"/>
    </filter>

    <filter id="ci-f-status-glow" x="-10%" y="-40%" width="120%" height="180%">
      <feGaussianBlur stdDeviation="2" result="blur"/>
      <feFlood flood-color="#A6E3A1" flood-opacity="0.5" result="color"/>
      <feComposite in="color" in2="blur" operator="in" result="glow"/>
      <feMerge><feMergeNode in="glow"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <filter id="ci-f-wheel" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur stdDeviation="1.5" result="blur"/>
      <feFlood flood-color="#A6E3A1" flood-opacity="0.5" result="color"/>
      <feComposite in="color" in2="blur" operator="in" result="glow"/>
      <feMerge><feMergeNode in="glow"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <filter id="ci-f-dot" x="-100%" y="-100%" width="300%" height="300%">
      <feGaussianBlur stdDeviation="2.5" result="blur"/>
      <feFlood flood-color="#A6E3A1" flood-opacity="0.7" result="color"/>
      <feComposite in="color" in2="blur" operator="in" result="glow"/>
      <feMerge><feMergeNode in="glow"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>

    <filter id="ci-f-burst" x="-80%" y="-80%" width="260%" height="260%">
      <feGaussianBlur stdDeviation="1.5"/>
    </filter>

    <!-- ════════════════════════════════════════════════════════════════════
         CLIP PATHS
         ════════════════════════════════════════════════════════════════════ -->

    <clipPath id="ci-clip-all">
      <rect x="0" y="0" width="150" height="28" rx="6"/>
    </clipPath>
    <clipPath id="ci-clip-label">
      <rect x="0" y="0" width="62" height="28"/>
    </clipPath>
    <clipPath id="ci-clip-value">
      <rect x="62" y="0" width="88" height="28"/>
    </clipPath>

  </defs>

  <!-- ════════════════════════════════════════════════════════════════════
       AMBIENT HALO
       ════════════════════════════════════════════════════════════════════ -->
  <ellipse class="ci-halo" cx="75" cy="14" rx="75" ry="14"
    fill="url(#ci-g-halo)"/>

  <!-- ════════════════════════════════════════════════════════════════════
       BADGE BODY
       ════════════════════════════════════════════════════════════════════ -->
  <g clip-path="url(#ci-clip-all)" filter="url(#ci-f-shadow)">

    <!-- ══════════════════════════════════════════════════════════════════
         LABEL SECTION
         ══════════════════════════════════════════════════════════════════ -->
    <g clip-path="url(#ci-clip-label)">

      <!-- Background -->
      <rect x="0" y="0" width="62" height="28" fill="url(#ci-g-label)"/>

      <!-- Glass top -->
      <rect x="0" y="0" width="62" height="13" fill="url(#ci-g-glass)"/>

      <!-- Shimmer -->
      <rect class="ci-shim-l"
        x="-80" y="0" width="36" height="28"
        fill="url(#ci-g-shimmer)"
        clip-path="url(#ci-clip-label)"
        opacity="0.4"/>

      <!-- ── BUILD WHEEL ICON ───────────────────────────────────────── -->
      <!-- Outer gear ring — slow CW -->
      <g class="ci-wheel" filter="url(#ci-f-wheel)">
        <!-- 8 spokes radiating from center -->
        <line x1="11" y1="6"  x2="11" y2="9"  stroke="#A6E3A1" stroke-width="2" stroke-linecap="round" opacity="0.8"/>
        <line x1="11" y1="19" x2="11" y2="22" stroke="#A6E3A1" stroke-width="2" stroke-linecap="round" opacity="0.8"/>
        <line x1="3"  y1="14" x2="6"  y2="14" stroke="#A6E3A1" stroke-width="2" stroke-linecap="round" opacity="0.8"/>
        <line x1="16" y1="14" x2="19" y2="14" stroke="#A6E3A1" stroke-width="2" stroke-linecap="round" opacity="0.8"/>
        <!-- Diagonal spokes -->
        <line x1="5.5"  y1="8.5"  x2="7.5"  y2="10.5" stroke="#A6E3A1" stroke-width="1.5" stroke-linecap="round" opacity="0.65"/>
        <line x1="14.5" y1="17.5" x2="16.5" y2="19.5" stroke="#A6E3A1" stroke-width="1.5" stroke-linecap="round" opacity="0.65"/>
        <line x1="5.5"  y1="19.5" x2="7.5"  y2="17.5" stroke="#A6E3A1" stroke-width="1.5" stroke-linecap="round" opacity="0.65"/>
        <line x1="14.5" y1="10.5" x2="16.5" y2="8.5"  stroke="#A6E3A1" stroke-width="1.5" stroke-linecap="round" opacity="0.65"/>
        <!-- Outer ring -->
        <circle cx="11" cy="14" r="7"
          fill="none" stroke="#A6E3A1" stroke-width="1.5" opacity="0.7"/>
      </g>

      <!-- Inner counter-rotating circle -->
      <g class="ci-wheel-inner">
        <circle cx="11" cy="14" r="3"
          fill="none" stroke="#94E2D5" stroke-width="1.5"
          stroke-dasharray="3 3" opacity="0.75"/>
      </g>

      <!-- Center dot (static) -->
      <circle cx="11" cy="14" r="2"
        fill="#A6E3A1" opacity="0.9"/>

      <!-- "build" label -->
      <text class="ci-label"
        x="24" y="18.5"
        font-family="'SF Mono','JetBrains Mono','Cascadia Code',monospace"
        font-size="9.5"
        font-weight="600"
        letter-spacing="0.5"
        fill="#A6ADC8">build</text>

    </g>

    <!-- ── DIVIDER ─────────────────────────────────────────────────────── -->
    <rect x="61" y="0" width="1" height="28" fill="#A6E3A1" opacity="0.35"/>

    <!-- ══════════════════════════════════════════════════════════════════
         VALUE SECTION
         ══════════════════════════════════════════════════════════════════ -->
    <g clip-path="url(#ci-clip-value)">

      <!-- Animated gradient base -->
      <rect x="62" y="0" width="88" height="28" fill="url(#ci-g-value)"/>
      <!-- Dark overlay -->
      <rect x="62" y="0" width="88" height="28" fill="url(#ci-g-value-dark)"/>
      <!-- Glass top -->
      <rect x="62" y="0" width="88" height="12" fill="url(#ci-g-glass)"/>

      <!-- ── PIPELINE PROGRESS BAR ──────────────────────────────────── -->
      <!-- Thin bar at very bottom showing 100% passed -->
      <rect x="62" y="26" width="88" height="1.5" rx="0.75"
        fill="url(#ci-g-progress)" opacity="0.5"/>
      <rect class="ci-progress"
        x="62" y="26" width="88" height="1.5" rx="0.75"
        fill="url(#ci-g-progress)" opacity="0.8"
        style="transform-origin:62px 26px"/>

      <!-- ── PIPELINE STAGE DOTS ────────────────────────────────────── -->
      <!-- 5 small dots showing build stages: lint · test · build · security · deploy -->
      <g transform="translate(65, 7)">
        <!-- Stage 1: lint -->
        <circle class="ci-stage-1" cx="0"  cy="0" r="2.5"
          fill="url(#ci-g-stage)" opacity="0.75"
          style="transform-origin:0px 0px"/>
        <!-- Connector line -->
        <line x1="2.5" y1="0" x2="8.5" y2="0"
          stroke="#A6E3A1" stroke-width="0.75" opacity="0.4"/>
        <!-- Stage 2: test -->
        <circle class="ci-stage-2" cx="11" cy="0" r="2.5"
          fill="url(#ci-g-stage)" opacity="0.75"
          style="transform-origin:11px 0px"/>
        <line x1="13.5" y1="0" x2="19.5" y2="0"
          stroke="#A6E3A1" stroke-width="0.75" opacity="0.4"/>
        <!-- Stage 3: build -->
        <circle class="ci-stage-3" cx="22" cy="0" r="2.5"
          fill="url(#ci-g-stage)" opacity="0.75"
          style="transform-origin:22px 0px"/>
        <line x1="24.5" y1="0" x2="30.5" y2="0"
          stroke="#A6E3A1" stroke-width="0.75" opacity="0.4"/>
        <!-- Stage 4: security -->
        <circle class="ci-stage-4" cx="33" cy="0" r="2.5"
          fill="url(#ci-g-stage)" opacity="0.75"
          style="transform-origin:33px 0px"/>
        <line x1="35.5" y1="0" x2="41.5" y2="0"
          stroke="#A6E3A1" stroke-width="0.75" opacity="0.4"/>
        <!-- Stage 5: deploy -->
        <circle class="ci-stage-5" cx="44" cy="0" r="2.5"
          fill="url(#ci-g-stage)" opacity="0.75"
          style="transform-origin:44px 0px"/>

        <!-- Stage labels (micro) -->
        <text x="0"  y="7.5" font-family="monospace" font-size="4" fill="#A6E3A1" opacity="0.5" text-anchor="middle">L</text>
        <text x="11" y="7.5" font-family="monospace" font-size="4" fill="#A6E3A1" opacity="0.5" text-anchor="middle">T</text>
        <text x="22" y="7.5" font-family="monospace" font-size="4" fill="#A6E3A1" opacity="0.5" text-anchor="middle">B</text>
        <text x="33" y="7.5" font-family="monospace" font-size="4" fill="#A6E3A1" opacity="0.5" text-anchor="middle">S</text>
        <text x="44" y="7.5" font-family="monospace" font-size="4" fill="#A6E3A1" opacity="0.5" text-anchor="middle">D</text>
      </g>

      <!-- Shimmer -->
      <rect class="ci-shim-r"
        x="22" y="0" width="30" height="28"
        fill="url(#ci-g-shimmer)"
        clip-path="url(#ci-clip-value)"
        opacity="0.5"/>

      <!-- ── STATUS TEXT ─────────────────────────────────────────────── -->
      <!-- "passing" status word -->
      <text class="ci-status-text"
        x="68" y="19.5"
        font-family="'SF Mono','JetBrains Mono','Cascadia Code',monospace"
        font-size="10.5"
        font-weight="700"
        letter-spacing="0.5"
        fill="#CDD6F4"
        filter="url(#ci-f-status-glow)">passing</text>

      <!-- ── CHECKMARK ICON ─────────────────────────────────────────── -->
      <g class="ci-tick" style="transform-origin:130px 14px">
        <!-- Circle border -->
        <circle cx="130" cy="14" r="8"
          fill="none"
          stroke="#A6E3A1"
          stroke-width="1.5"
          opacity="0.8"/>
        <!-- Green fill -->
        <circle cx="130" cy="14" r="6.5"
          fill="#A6E3A1"
          opacity="0.2"/>
        <!-- Checkmark path — animated draw -->
        <path class="ci-check"
          d="M 126 14 L 129 17 L 134 11"
          fill="none"
          stroke="#A6E3A1"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-dasharray="20"
          stroke-dashoffset="20"
          opacity="0.95"/>
      </g>

      <!-- ── STATUS INDICATOR DOT ──────────────────────────────────── -->
      <!-- Pulsing green dot — the main live indicator -->
      <circle class="ci-dot ci-state"
        cx="128" cy="14" r="5"
        fill="none"
        stroke="#A6E3A1"
        stroke-width="1.5"
        opacity="0">
        <animate attributeName="r"       values="5;12;5"   dur="2s" repeatCount="indefinite"/>
        <animate attributeName="opacity" values="0.6;0;0.6" dur="2s" repeatCount="indefinite"/>
      </circle>

      <!-- Matrix rain beads — falling green data chars -->
      <g filter="url(#ci-f-burst)" opacity="0.4">
        <text class="ci-bead-1"
          x="143" y="0"
          font-family="monospace"
          font-size="6"
          fill="#A6E3A1">1</text>
        <text class="ci-bead-2"
          x="147" y="0"
          font-family="monospace"
          font-size="6"
          fill="#94E2D5">0</text>
        <text class="ci-bead-3"
          x="143" y="0"
          font-family="monospace"
          font-size="6"
          fill="#89DCEB">1</text>
      </g>

      <!-- Run count display (small, bottom-right) -->
      <g class="ci-run-count" transform="translate(0, 1)">
        <text x="118" y="24"
          font-family="'SF Mono',monospace"
          font-size="6"
          letter-spacing="0.5"
          fill="#A6E3A1"
          opacity="0.45">#4,821</text>
      </g>

    </g>

    <!-- ── OUTER BORDER ───────────────────────────────────────────────── -->
    <rect x="0.5" y="0.5" width="149" height="27" rx="5.5"
      fill="none"
      stroke="url(#ci-g-border)"
      stroke-width="1"/>

    <!-- ── TOP ACCENT BAR ─────────────────────────────────────────────── -->
    <rect class="ci-bar"
      x="0" y="0" width="150" height="1.5" rx="0.75"
      fill="url(#ci-g-value)"
      opacity="0.85"
      style="transform-origin:0 0"/>

    <!-- ── LEFT EDGE ACCENT ───────────────────────────────────────────── -->
    <rect x="0" y="0" width="1.5" height="28" rx="0.75"
      fill="#A6E3A1" opacity="0.3"/>

    <!-- ── BOTTOM SCAN LINE ───────────────────────────────────────────── -->
    <rect x="0" y="26.5" width="150" height="0.75" rx="0.375"
      fill="#A6E3A1" opacity="0.2"/>

  </g>

  <!-- ════════════════════════════════════════════════════════════════════
       SUCCESS BURST PARTICLES
       ════════════════════════════════════════════════════════════════════ -->
  <circle class="ci-burst-1" cx="131" cy="22" r="2.2"
    fill="url(#ci-bf1)" style="--bx:3px;--by:-9px;--bx2:6px;--by2:-17px"/>
  <circle class="ci-burst-2" cx="135" cy="18" r="1.8"
    fill="url(#ci-bf2)" style="--bx:-4px;--by:-8px;--bx2:-7px;--by2:-15px"/>
  <circle class="ci-burst-3" cx="128" cy="24" r="2"
    fill="url(#ci-bf3)" style="--bx:5px;--by:-10px;--bx2:9px;--by2:-19px"/>
  <circle class="ci-burst-4" cx="133" cy="26" r="1.5"
    fill="url(#ci-bf4)" style="--bx:-3px;--by:-11px;--bx2:-6px;--by2:-21px"/>
  <circle class="ci-burst-5" cx="138" cy="20" r="1.9"
    fill="url(#ci-bf5)" style="--bx:4px;--by:-8px;--bx2:7px;--by2:-16px"/>

</svg>