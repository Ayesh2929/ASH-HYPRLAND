<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<!--
╔══════════════════════════════════════════════════════════════════════════════╗
║  🌊 ASH DOTFILES v5.0 OMEGA — PRIMARY LOGO (ADAPTIVE)                      ║
║  Auto-adapts to system color scheme via prefers-color-scheme                ║
║  Formats: SVG 1.1 · Scalable · Accessible · Animated                       ║
║  Designer: ASH Design Team · License: MIT                                  ║
╚══════════════════════════════════════════════════════════════════════════════╝
-->
<svg
  xmlns="http://www.w3.org/2000/svg"
  xmlns:xlink="http://www.w3.org/1999/xlink"
  viewBox="0 0 480 160"
  width="480"
  height="160"
  role="img"
  aria-labelledby="ash-logo-title ash-logo-desc"
  aria-label="ASH Dotfiles Logo">

  <!-- ── ACCESSIBILITY ─────────────────────────────────────────────────── -->
  <title id="ash-logo-title">ASH Dotfiles</title>
  <desc id="ash-logo-desc">
    ASH Dotfiles v5.0 OMEGA — The most advanced Hyprland dotfiles repository.
    Logo featuring stylized wave mark and wordmark.
  </desc>

  <!-- ── METADATA ──────────────────────────────────────────────────────── -->
  <metadata>
    <rdf:RDF xmlns:rdf="http://www.w3.org/1999/02/22-rdf-syntax-ns#"
             xmlns:dc="http://purl.org/dc/elements/1.1/"
             xmlns:cc="http://creativecommons.org/ns#">
      <cc:Work rdf:about="">
        <dc:title>ASH Dotfiles Logo</dc:title>
        <dc:creator>ASH Design Team</dc:creator>
        <dc:description>Primary adaptive logo for ASH Dotfiles v5.0 OMEGA</dc:description>
        <dc:format>image/svg+xml</dc:format>
        <dc:type rdf:resource="http://purl.org/dc/dcmitype/StillImage"/>
        <cc:license rdf:resource="http://opensource.org/licenses/MIT"/>
      </cc:Work>
    </rdf:RDF>
  </metadata>

  <!-- ══════════════════════════════════════════════════════════════════════
       DEFINITIONS — Gradients · Filters · Animations · Masks
       ══════════════════════════════════════════════════════════════════════ -->
  <defs>

    <!-- ── ADAPTIVE COLOR SCHEME ─────────────────────────────────────── -->
    <style>
      :root {
        --ash-bg:           transparent;
        --ash-text:         #CDD6F4;
        --ash-text-dim:     #A6ADC8;
        --ash-accent-1:     #89B4FA;
        --ash-accent-2:     #CBA6F7;
        --ash-accent-3:     #89DCEB;
        --ash-glow-1:       #89B4FA;
        --ash-glow-2:       #CBA6F7;
        --ash-wave-1:       #89B4FA;
        --ash-wave-2:       #74C7EC;
        --ash-wave-3:       #89DCEB;
        --ash-stroke:       rgba(137,180,250,0.3);
        --ash-shadow:       rgba(0,0,0,0.4);
        --ash-glass:        rgba(30,30,46,0.6);
        --ash-version-bg:   rgba(137,180,250,0.15);
        --ash-version-text: #89B4FA;
        --ash-tagline:      #6C7086;
      }

      @media (prefers-color-scheme: light) {
        :root {
          --ash-text:         #1E1E2E;
          --ash-text-dim:     #4C4F69;
          --ash-accent-1:     #1E66F5;
          --ash-accent-2:     #8839EF;
          --ash-accent-3:     #04A5E5;
          --ash-glow-1:       #1E66F5;
          --ash-glow-2:       #8839EF;
          --ash-wave-1:       #1E66F5;
          --ash-wave-2:       #04A5E5;
          --ash-wave-3:       #179299;
          --ash-stroke:       rgba(30,102,245,0.2);
          --ash-shadow:       rgba(0,0,0,0.12);
          --ash-glass:        rgba(239,241,245,0.8);
          --ash-version-bg:   rgba(30,102,245,0.1);
          --ash-version-text: #1E66F5;
          --ash-tagline:      #8C8FA1;
        }
      }

      /* ── ANIMATION CLASSES ──────────────────────────────────────────── */
      .ash-wave-path {
        animation: ash-wave-drift 6s ease-in-out infinite;
        transform-origin: center;
      }
      .ash-wave-path-2 {
        animation: ash-wave-drift 8s ease-in-out infinite reverse;
        transform-origin: center;
      }
      .ash-wave-path-3 {
        animation: ash-wave-drift 10s ease-in-out infinite;
        transform-origin: center;
      }
      .ash-glow-ring {
        animation: ash-pulse 3s ease-in-out infinite;
        transform-origin: 80px 80px;
      }
      .ash-letter-a { animation: ash-letter-rise 0.8s cubic-bezier(0.34,1.56,0.64,1) both; }
      .ash-letter-s { animation: ash-letter-rise 0.8s cubic-bezier(0.34,1.56,0.64,1) 0.1s both; }
      .ash-letter-h { animation: ash-letter-rise 0.8s cubic-bezier(0.34,1.56,0.64,1) 0.2s both; }
      .ash-tagline   { animation: ash-fade-up 1s ease-out 0.6s both; }
      .ash-version   { animation: ash-fade-up 1s ease-out 0.8s both; }
      .ash-particle  { animation: ash-float 4s ease-in-out infinite; }
      .ash-mark-ring { animation: ash-spin-slow 20s linear infinite; }

      /* Respect reduced motion */
      @media (prefers-reduced-motion: reduce) {
        .ash-wave-path,
        .ash-wave-path-2,
        .ash-wave-path-3,
        .ash-glow-ring,
        .ash-letter-a,
        .ash-letter-s,
        .ash-letter-h,
        .ash-tagline,
        .ash-version,
        .ash-particle,
        .ash-mark-ring {
          animation: none !important;
        }
      }

      @keyframes ash-wave-drift {
        0%, 100% { d: path("M 20 80 C 35 60, 55 100, 70 80 C 85 60, 105 100, 120 80"); }
        50%       { d: path("M 20 80 C 35 95, 55 65, 70 80 C 85 95, 105 65, 120 80"); }
      }

      @keyframes ash-pulse {
        0%, 100% { opacity: 0.4; transform: scale(1);    }
        50%       { opacity: 0.8; transform: scale(1.06); }
      }

      @keyframes ash-letter-rise {
        from { opacity: 0; transform: translateY(12px); }
        to   { opacity: 1; transform: translateY(0);    }
      }

      @keyframes ash-fade-up {
        from { opacity: 0; transform: translateY(6px); }
        to   { opacity: 1; transform: translateY(0);   }
      }

      @keyframes ash-float {
        0%, 100% { transform: translateY(0)   rotate(0deg);   opacity: 0.6; }
        33%       { transform: translateY(-6px) rotate(120deg); opacity: 1;   }
        66%       { transform: translateY(4px)  rotate(240deg); opacity: 0.8; }
      }

      @keyframes ash-spin-slow {
        from { transform: rotate(0deg);   }
        to   { transform: rotate(360deg); }
      }
    </style>

    <!-- ── PRIMARY GRADIENT — Blue→Purple wave ───────────────────────── -->
    <linearGradient id="grad-primary" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#89B4FA" stop-opacity="1">
        <animate attributeName="stop-color"
          values="#89B4FA;#CBA6F7;#89DCEB;#89B4FA"
          dur="8s" repeatCount="indefinite"/>
      </stop>
      <stop offset="50%"  stop-color="#CBA6F7" stop-opacity="1">
        <animate attributeName="stop-color"
          values="#CBA6F7;#89DCEB;#89B4FA;#CBA6F7"
          dur="8s" repeatCount="indefinite"/>
      </stop>
      <stop offset="100%" stop-color="#89DCEB" stop-opacity="1">
        <animate attributeName="stop-color"
          values="#89DCEB;#89B4FA;#CBA6F7;#89DCEB"
          dur="8s" repeatCount="indefinite"/>
      </stop>
    </linearGradient>

    <!-- ── WAVE GRADIENT ─────────────────────────────────────────────── -->
    <linearGradient id="grad-wave" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#89B4FA" stop-opacity="0.9"/>
      <stop offset="40%"  stop-color="#74C7EC" stop-opacity="0.95"/>
      <stop offset="100%" stop-color="#89DCEB" stop-opacity="0.8"/>
    </linearGradient>

    <!-- ── WAVE GRADIENT 2 ───────────────────────────────────────────── -->
    <linearGradient id="grad-wave-2" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#CBA6F7" stop-opacity="0.7"/>
      <stop offset="50%"  stop-color="#89B4FA" stop-opacity="0.8"/>
      <stop offset="100%" stop-color="#74C7EC" stop-opacity="0.6"/>
    </linearGradient>

    <!-- ── TEXT GRADIENT ─────────────────────────────────────────────── -->
    <linearGradient id="grad-text" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#CDD6F4"/>
      <stop offset="100%" stop-color="#BAC2DE"/>
    </linearGradient>

    <!-- ── GLOW GRADIENT ─────────────────────────────────────────────── -->
    <radialGradient id="grad-glow" cx="50%" cy="50%" r="50%">
      <stop offset="0%"   stop-color="#89B4FA" stop-opacity="0.5"/>
      <stop offset="60%"  stop-color="#CBA6F7" stop-opacity="0.2"/>
      <stop offset="100%" stop-color="#89B4FA" stop-opacity="0"/>
    </radialGradient>

    <!-- ── MARK BACKGROUND GRADIENT ─────────────────────────────────── -->
    <radialGradient id="grad-mark-bg" cx="50%" cy="40%" r="60%">
      <stop offset="0%"   stop-color="#313244" stop-opacity="1"/>
      <stop offset="100%" stop-color="#1E1E2E" stop-opacity="1"/>
    </radialGradient>

    <!-- ── GLASS GRADIENT ────────────────────────────────────────────── -->
    <linearGradient id="grad-glass" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%"   stop-color="#585B70" stop-opacity="0.3"/>
      <stop offset="100%" stop-color="#313244" stop-opacity="0.1"/>
    </linearGradient>

    <!-- ── PARTICLE GRADIENT ─────────────────────────────────────────── -->
    <radialGradient id="grad-particle-1" cx="50%" cy="50%" r="50%">
      <stop offset="0%"   stop-color="#89B4FA" stop-opacity="1"/>
      <stop offset="100%" stop-color="#89B4FA" stop-opacity="0"/>
    </radialGradient>
    <radialGradient id="grad-particle-2" cx="50%" cy="50%" r="50%">
      <stop offset="0%"   stop-color="#CBA6F7" stop-opacity="1"/>
      <stop offset="100%" stop-color="#CBA6F7" stop-opacity="0"/>
    </radialGradient>
    <radialGradient id="grad-particle-3" cx="50%" cy="50%" r="50%">
      <stop offset="0%"   stop-color="#A6E3A1" stop-opacity="1"/>
      <stop offset="100%" stop-color="#A6E3A1" stop-opacity="0"/>
    </radialGradient>

    <!-- ── SHADOW FILTER ─────────────────────────────────────────────── -->
    <filter id="filter-shadow" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow
        dx="0" dy="4" stdDeviation="8"
        flood-color="#000000" flood-opacity="0.35"/>
    </filter>

    <!-- ── GLOW FILTER ───────────────────────────────────────────────── -->
    <filter id="filter-glow" x="-30%" y="-30%" width="160%" height="160%">
      <feGaussianBlur stdDeviation="3" result="blur"/>
      <feFlood flood-color="#89B4FA" flood-opacity="0.6" result="color"/>
      <feComposite in="color" in2="blur" operator="in" result="glow"/>
      <feMerge>
        <feMergeNode in="glow"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <!-- ── STRONG GLOW FILTER ───────────────────────────────────────── -->
    <filter id="filter-glow-strong" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur stdDeviation="6" result="blur1"/>
      <feGaussianBlur stdDeviation="12" result="blur2"/>
      <feMerge>
        <feMergeNode in="blur2"/>
        <feMergeNode in="blur1"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <!-- ── BLUR FILTER ───────────────────────────────────────────────── -->
    <filter id="filter-blur-soft">
      <feGaussianBlur stdDeviation="1.5"/>
    </filter>

    <!-- ── MARK CLIP PATH ────────────────────────────────────────────── -->
    <clipPath id="clip-mark">
      <circle cx="80" cy="80" r="58"/>
    </clipPath>

    <!-- ── WAVE MASK ─────────────────────────────────────────────────── -->
    <mask id="mask-wave-fade">
      <rect x="22" y="30" width="116" height="100" fill="white"/>
      <rect x="22" y="30" width="10"  height="100" fill="url(#grad-fade-l)"/>
      <rect x="128" y="30" width="10" height="100" fill="url(#grad-fade-r)"/>
    </mask>

    <linearGradient id="grad-fade-l" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="black"/>
      <stop offset="100%" stop-color="white"/>
    </linearGradient>
    <linearGradient id="grad-fade-r" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="white"/>
      <stop offset="100%" stop-color="black"/>
    </linearGradient>

  </defs>

  <!-- ══════════════════════════════════════════════════════════════════════
       LAYER 1 — BACKGROUND
       ══════════════════════════════════════════════════════════════════════ -->
  <g id="layer-background">
    <!-- Subtle ambient glow behind mark -->
    <ellipse cx="80" cy="80" rx="72" ry="72"
      fill="url(#grad-glow)" opacity="0.4">
      <animate attributeName="rx" values="72;80;72" dur="4s" repeatCount="indefinite"/>
      <animate attributeName="ry" values="72;80;72" dur="4s" repeatCount="indefinite"/>
    </ellipse>
  </g>

  <!-- ══════════════════════════════════════════════════════════════════════
       LAYER 2 — MARK (Left side icon, 160×160 canvas)
       ══════════════════════════════════════════════════════════════════════ -->
  <g id="layer-mark" filter="url(#filter-shadow)">

    <!-- Outer decorative ring (slow spin) -->
    <g class="ash-mark-ring" style="transform-origin:80px 80px">
      <circle cx="80" cy="80" r="70"
        fill="none"
        stroke="url(#grad-primary)"
        stroke-width="1"
        stroke-dasharray="8 6"
        opacity="0.25"/>
    </g>

    <!-- Main circular background -->
    <circle cx="80" cy="80" r="62"
      fill="url(#grad-mark-bg)"
      stroke="url(#grad-primary)"
      stroke-width="2"/>

    <!-- Glass highlight overlay -->
    <ellipse cx="80" cy="54" rx="44" ry="28"
      fill="url(#grad-glass)" opacity="0.4"/>

    <!-- Inner content clipped to circle -->
    <g clip-path="url(#clip-mark)">

      <!-- ── WAVE SYSTEM ───────────────────────────────────────────── -->
      <!-- Wave 3 — back layer (cyan, slow) -->
      <path class="ash-wave-path-3"
        d="M 22 88
           C 35 72, 52 104, 66 88
           C 80 72, 94 104, 108 88
           C 118 72, 128 90, 138 84"
        fill="none"
        stroke="#89DCEB"
        stroke-width="2.5"
        stroke-linecap="round"
        opacity="0.35"
        mask="url(#mask-wave-fade)"/>

      <!-- Wave 2 — mid layer (purple) -->
      <path class="ash-wave-path-2"
        d="M 22 80
           C 36 62, 54 98, 70 80
           C 86 62, 102 98, 118 80
           C 126 66, 132 86, 138 76"
        fill="none"
        stroke="url(#grad-wave-2)"
        stroke-width="3"
        stroke-linecap="round"
        opacity="0.55"
        mask="url(#mask-wave-fade)"/>

      <!-- Wave 1 — front layer (blue, primary) -->
      <path class="ash-wave-path"
        d="M 22 72
           C 36 54, 56 90, 72 72
           C 88 54, 104 90, 120 72
           C 128 60, 134 76, 138 68"
        fill="none"
        stroke="url(#grad-wave)"
        stroke-width="4.5"
        stroke-linecap="round"
        filter="url(#filter-glow)"
        mask="url(#mask-wave-fade)"/>

      <!-- Wave fill area (subtle) -->
      <path
        d="M 22 80
           C 36 62, 54 98, 70 80
           C 86 62, 102 98, 118 80
           C 126 66, 132 86, 138 76
           L 138 138 L 22 138 Z"
        fill="url(#grad-wave)"
        opacity="0.06"/>

    </g>

    <!-- ── LETTER MARK — "A" ─────────────────────────────────────── -->
    <!-- Centered ASH letter with gradient and glow -->
    <text
      x="80" y="92"
      font-family="'SF Pro Display', 'Inter', 'Segoe UI', system-ui, sans-serif"
      font-size="52"
      font-weight="800"
      font-style="normal"
      letter-spacing="-2"
      text-anchor="middle"
      fill="url(#grad-primary)"
      filter="url(#filter-glow)"
      opacity="0.95">A</text>

    <!-- Letter shadow for depth -->
    <text
      x="80" y="92"
      font-family="'SF Pro Display', 'Inter', 'Segoe UI', system-ui, sans-serif"
      font-size="52"
      font-weight="800"
      letter-spacing="-2"
      text-anchor="middle"
      fill="none"
      stroke="#89B4FA"
      stroke-width="0.5"
      opacity="0.3">A</text>

    <!-- ── ORBIT DOTS ────────────────────────────────────────────── -->
    <circle cx="80" cy="22" r="3.5"
      fill="#89B4FA" opacity="0.8"
      filter="url(#filter-glow)">
      <animateTransform attributeName="transform" type="rotate"
        from="0 80 80" to="360 80 80" dur="6s" repeatCount="indefinite"/>
    </circle>

    <circle cx="80" cy="22" r="2"
      fill="#CBA6F7" opacity="0.6"
      filter="url(#filter-glow)">
      <animateTransform attributeName="transform" type="rotate"
        from="120 80 80" to="480 80 80" dur="9s" repeatCount="indefinite"/>
    </circle>

    <circle cx="80" cy="22" r="1.5"
      fill="#A6E3A1" opacity="0.5"
      filter="url(#filter-glow)">
      <animateTransform attributeName="transform" type="rotate"
        from="240 80 80" to="-120 80 80" dur="12s" repeatCount="indefinite"/>
    </circle>

    <!-- ── GLOW RING (pulse) ─────────────────────────────────────── -->
    <circle class="ash-glow-ring" cx="80" cy="80" r="62"
      fill="none"
      stroke="url(#grad-primary)"
      stroke-width="1.5"
      opacity="0.4"/>

  </g>

  <!-- ══════════════════════════════════════════════════════════════════════
       LAYER 3 — FLOATING PARTICLES
       ══════════════════════════════════════════════════════════════════════ -->
  <g id="layer-particles" opacity="0.7">

    <circle class="ash-particle" cx="140" cy="42" r="3"
      fill="url(#grad-particle-1)"
      style="animation-delay: 0s; animation-duration: 5.2s"/>

    <circle class="ash-particle" cx="148" cy="118" r="2"
      fill="url(#grad-particle-2)"
      style="animation-delay: 1.4s; animation-duration: 4.8s"/>

    <circle class="ash-particle" cx="152" cy="78" r="1.5"
      fill="url(#grad-particle-3)"
      style="animation-delay: 2.8s; animation-duration: 6.1s"/>

    <circle class="ash-particle" cx="155" cy="55" r="2.5"
      fill="url(#grad-particle-1)"
      style="animation-delay: 0.7s; animation-duration: 7.3s"/>

    <circle class="ash-particle" cx="143" cy="98" r="1.8"
      fill="url(#grad-particle-2)"
      style="animation-delay: 3.2s; animation-duration: 5.7s"/>

  </g>

  <!-- ══════════════════════════════════════════════════════════════════════
       LAYER 4 — WORDMARK
       ══════════════════════════════════════════════════════════════════════ -->
  <g id="layer-wordmark" transform="translate(172, 0)">

    <!-- ── LETTERS: ASH ──────────────────────────────────────────── -->

    <!-- Letter A -->
    <g class="ash-letter-a">
      <text
        x="0" y="92"
        font-family="'SF Pro Display', 'Inter', 'Helvetica Neue', 'Segoe UI', system-ui, sans-serif"
        font-size="72"
        font-weight="900"
        letter-spacing="-3"
        fill="url(#grad-text)"
        dominant-baseline="auto">A</text>
    </g>

    <!-- Letter S -->
    <g class="ash-letter-s">
      <text
        x="52" y="92"
        font-family="'SF Pro Display', 'Inter', 'Helvetica Neue', 'Segoe UI', system-ui, sans-serif"
        font-size="72"
        font-weight="900"
        letter-spacing="-3"
        fill="url(#grad-primary)"
        filter="url(#filter-glow)"
        dominant-baseline="auto">S</text>
    </g>

    <!-- Letter H -->
    <g class="ash-letter-h">
      <text
        x="102" y="92"
        font-family="'SF Pro Display', 'Inter', 'Helvetica Neue', 'Segoe UI', system-ui, sans-serif"
        font-size="72"
        font-weight="900"
        letter-spacing="-3"
        fill="url(#grad-text)"
        dominant-baseline="auto">H</text>
    </g>

    <!-- ── VERTICAL SEPARATOR ────────────────────────────────────── -->
    <line x1="160" y1="30" x2="160" y2="110"
      stroke="url(#grad-primary)"
      stroke-width="1.5"
      opacity="0.3"
      stroke-linecap="round"/>

    <!-- ── TAGLINE ────────────────────────────────────────────────── -->
    <g class="ash-tagline">
      <text
        x="170" y="60"
        font-family="'SF Mono', 'JetBrains Mono', 'Fira Code', 'Cascadia Code', monospace"
        font-size="10"
        font-weight="500"
        letter-spacing="3"
        fill="#6C7086"
        dominant-baseline="auto">DOTFILES</text>

      <text
        x="170" y="78"
        font-family="'SF Mono', 'JetBrains Mono', 'Fira Code', 'Cascadia Code', monospace"
        font-size="10"
        font-weight="400"
        letter-spacing="2"
        fill="#585B70"
        dominant-baseline="auto">v5.0 OMEGA</text>

      <!-- ── FEATURE DOTS ──────────────────────────────────────── -->
      <g transform="translate(170, 92)">
        <!-- Dot 1 — Themes -->
        <circle cx="0"  cy="0" r="3" fill="#89B4FA" opacity="0.9"/>
        <circle cx="12" cy="0" r="3" fill="#CBA6F7" opacity="0.9"/>
        <circle cx="24" cy="0" r="3" fill="#A6E3A1" opacity="0.9"/>
        <circle cx="36" cy="0" r="3" fill="#F38BA8" opacity="0.9"/>
        <circle cx="48" cy="0" r="3" fill="#FAB387" opacity="0.9"/>
        <circle cx="60" cy="0" r="3" fill="#89DCEB" opacity="0.9"/>
        <circle cx="72" cy="0" r="3" fill="#F9E2AF" opacity="0.9"/>

        <!-- Animated pulse on first dot -->
        <circle cx="0" cy="0" r="3" fill="#89B4FA" opacity="0">
          <animate attributeName="r"       values="3;8;3"   dur="2s" repeatCount="indefinite"/>
          <animate attributeName="opacity" values="0.8;0;0.8" dur="2s" repeatCount="indefinite"/>
        </circle>
      </g>

      <!-- ── MICRO LABELS ──────────────────────────────────────── -->
      <text
        x="170" y="114"
        font-family="'SF Mono', 'JetBrains Mono', monospace"
        font-size="7.5"
        font-weight="400"
        letter-spacing="1.5"
        fill="#45475A"
        dominant-baseline="auto">250+ THEMES · 150+ PLUGINS · AI-NATIVE</text>
    </g>

    <!-- ── VERSION BADGE ─────────────────────────────────────────── -->
    <g class="ash-version" transform="translate(152, 118)">
      <!-- Badge background -->
      <rect x="0" y="0" width="120" height="20" rx="10"
        fill="rgba(137,180,250,0.12)"
        stroke="#89B4FA"
        stroke-width="0.75"
        stroke-opacity="0.4"/>
      <!-- Badge text -->
      <text
        x="60" y="14"
        font-family="'SF Mono', 'JetBrains Mono', monospace"
        font-size="8.5"
        font-weight="600"
        letter-spacing="1.5"
        fill="#89B4FA"
        text-anchor="middle"
        dominant-baseline="auto">⚡ HYPRLAND NATIVE</text>
    </g>

  </g>

  <!-- ══════════════════════════════════════════════════════════════════════
       LAYER 5 — ACCESSIBILITY OVERLAY (screen-reader only)
       ══════════════════════════════════════════════════════════════════════ -->
  <text
    x="-9999" y="-9999"
    aria-hidden="false"
    role="presentation">
    ASH Dotfiles — The most advanced Hyprland dotfiles repository.
    Version 5.0 OMEGA. Features 250+ themes, 150+ plugins, AI-native theming.
  </text>

</svg>