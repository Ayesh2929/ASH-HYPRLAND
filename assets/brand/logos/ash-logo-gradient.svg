<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<!--
╔══════════════════════════════════════════════════════════════════════════════╗
║  🌈 ASH DOTFILES v5.0 OMEGA — GRADIENT LOGO                                ║
║  Spectacular multi-stop gradient with iridescent color shifting             ║
║  Perfect for: hero sections · splash screens · marketing · social media     ║
║  Palette: Aurora · Nebula · Cosmic · Prism                                  ║
╚══════════════════════════════════════════════════════════════════════════════╝
-->
<svg
  xmlns="http://www.w3.org/2000/svg"
  xmlns:xlink="http://www.w3.org/1999/xlink"
  viewBox="0 0 520 180"
  width="520"
  height="180"
  role="img"
  aria-labelledby="ash-grad-title ash-grad-desc">

  <title id="ash-grad-title">ASH Dotfiles — Gradient Logo</title>
  <desc id="ash-grad-desc">
    ASH Dotfiles v5.0 OMEGA gradient logo with iridescent aurora color scheme.
    Features animated color cycling across the full spectrum.
  </desc>

  <defs>

    <!-- ══════════════════════════════════════════════════════════════════
         STYLE & ANIMATIONS
         ══════════════════════════════════════════════════════════════════ -->
    <style><![CDATA[
      /* ── Reset ──────────────────────────────────────────────────────── */
      * { box-sizing: border-box; }

      /* ── Gradient cycling ───────────────────────────────────────────── */
      .gr-cycle-fast   { animation: gr-hue-cycle  4s linear infinite; }
      .gr-cycle-med    { animation: gr-hue-cycle  7s linear infinite reverse; }
      .gr-cycle-slow   { animation: gr-hue-cycle 12s linear infinite 2s; }

      /* ── Wave animations ─────────────────────────────────────────────  */
      .gr-wave-a { animation: gr-wave-morph  8s ease-in-out infinite; }
      .gr-wave-b { animation: gr-wave-morph  10s ease-in-out infinite reverse 1s; }
      .gr-wave-c { animation: gr-wave-morph  13s ease-in-out infinite 2s; }
      .gr-wave-d { animation: gr-wave-morph  6s ease-in-out infinite 0.5s reverse; }

      /* ── Mark animations ─────────────────────────────────────────────  */
      .gr-mark-spin    { animation: gr-spin-cw  25s linear infinite; transform-origin: 90px 90px; }
      .gr-mark-spin-rv { animation: gr-spin-ccw 18s linear infinite; transform-origin: 90px 90px; }
      .gr-mark-pulse   { animation: gr-pulse    4s ease-in-out infinite; transform-origin: 90px 90px; }
      .gr-mark-float   { animation: gr-float    6s ease-in-out infinite; }

      /* ── Letter animations ───────────────────────────────────────────  */
      .gr-letter       { animation: gr-letter-shimmer 5s ease-in-out infinite; }
      .gr-letter-a     { animation-delay: 0s;   }
      .gr-letter-s     { animation-delay: 0.3s; }
      .gr-letter-h     { animation-delay: 0.6s; }

      /* ── Particle system ─────────────────────────────────────────────  */
      .gr-p1  { animation: gr-particle 5.1s ease-in-out infinite; }
      .gr-p2  { animation: gr-particle 6.3s ease-in-out infinite 0.8s; }
      .gr-p3  { animation: gr-particle 4.7s ease-in-out infinite 1.6s; }
      .gr-p4  { animation: gr-particle 7.2s ease-in-out infinite 2.4s; }
      .gr-p5  { animation: gr-particle 5.8s ease-in-out infinite 0.4s; }
      .gr-p6  { animation: gr-particle 4.2s ease-in-out infinite 3.2s; }
      .gr-p7  { animation: gr-particle 6.9s ease-in-out infinite 1.1s; }
      .gr-p8  { animation: gr-particle 5.5s ease-in-out infinite 2.8s; }

      /* ── Shimmer ─────────────────────────────────────────────────────  */
      .gr-shimmer      { animation: gr-shimmer-sweep 3s ease-in-out infinite; }
      .gr-shimmer-2    { animation: gr-shimmer-sweep 4s ease-in-out infinite 1.5s; }

      /* ── Tagline ─────────────────────────────────────────────────────  */
      .gr-tagline      { animation: gr-tagline-glow  3s ease-in-out infinite; }

      /* ── Reduced motion ─────────────────────────────────────────────  */
      @media (prefers-reduced-motion: reduce) {
        * { animation: none !important; transition: none !important; }
      }

      /* ══════════════════════════════════════════════════════════════════
         KEYFRAMES
         ══════════════════════════════════════════════════════════════════ */

      @keyframes gr-hue-cycle {
        0%   { filter: hue-rotate(0deg)   saturate(1.2); }
        25%  { filter: hue-rotate(60deg)  saturate(1.4); }
        50%  { filter: hue-rotate(180deg) saturate(1.1); }
        75%  { filter: hue-rotate(270deg) saturate(1.3); }
        100% { filter: hue-rotate(360deg) saturate(1.2); }
      }

      @keyframes gr-wave-morph {
        0%   { transform: translateY(0px)  scaleX(1);    }
        25%  { transform: translateY(-5px) scaleX(1.02); }
        50%  { transform: translateY(3px)  scaleX(0.98); }
        75%  { transform: translateY(-2px) scaleX(1.01); }
        100% { transform: translateY(0px)  scaleX(1);    }
      }

      @keyframes gr-spin-cw {
        from { transform: rotate(0deg);   }
        to   { transform: rotate(360deg); }
      }

      @keyframes gr-spin-ccw {
        from { transform: rotate(0deg);    }
        to   { transform: rotate(-360deg); }
      }

      @keyframes gr-pulse {
        0%, 100% { transform: scale(1);    opacity: 1;   }
        50%       { transform: scale(1.04); opacity: 0.9; }
      }

      @keyframes gr-float {
        0%, 100% { transform: translateY(0px);  }
        50%       { transform: translateY(-6px); }
      }

      @keyframes gr-letter-shimmer {
        0%, 100% { opacity: 1;   filter: brightness(1);   }
        50%       { opacity: 0.9; filter: brightness(1.15); }
      }

      @keyframes gr-particle {
        0%   { transform: translate(0, 0)     scale(1);    opacity: 0;   }
        10%  { opacity: 1; }
        50%  { transform: translate(var(--px,8px), var(--py,-20px)) scale(1.2); opacity: 0.8; }
        90%  { opacity: 0.2; }
        100% { transform: translate(var(--px2,12px), var(--py2,-35px)) scale(0.4); opacity: 0; }
      }

      @keyframes gr-shimmer-sweep {
        0%   { transform: translateX(-120px) skewX(-15deg); opacity: 0;   }
        30%  { opacity: 0.6; }
        70%  { opacity: 0.6; }
        100% { transform: translateX(120px)  skewX(-15deg); opacity: 0;   }
      }

      @keyframes gr-tagline-glow {
        0%, 100% { opacity: 0.7; }
        50%       { opacity: 1;   }
      }
    ]]></style>

    <!-- ══════════════════════════════════════════════════════════════════
         GRADIENT LIBRARY
         ══════════════════════════════════════════════════════════════════ -->

    <!-- ── AURORA — primary iridescent gradient ──────────────────────── -->
    <linearGradient id="gr-aurora" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"    stop-color="#FF6B9D">
        <animate attributeName="stop-color"
          values="#FF6B9D;#C77DFF;#74C7EC;#A6E3A1;#F9E2AF;#FF6B9D"
          dur="8s" repeatCount="indefinite"/>
      </stop>
      <stop offset="20%"   stop-color="#C77DFF">
        <animate attributeName="stop-color"
          values="#C77DFF;#74C7EC;#A6E3A1;#F9E2AF;#FF6B9D;#C77DFF"
          dur="8s" repeatCount="indefinite"/>
      </stop>
      <stop offset="40%"   stop-color="#74C7EC">
        <animate attributeName="stop-color"
          values="#74C7EC;#A6E3A1;#F9E2AF;#FF6B9D;#C77DFF;#74C7EC"
          dur="8s" repeatCount="indefinite"/>
      </stop>
      <stop offset="60%"   stop-color="#A6E3A1">
        <animate attributeName="stop-color"
          values="#A6E3A1;#F9E2AF;#FF6B9D;#C77DFF;#74C7EC;#A6E3A1"
          dur="8s" repeatCount="indefinite"/>
      </stop>
      <stop offset="80%"   stop-color="#F9E2AF">
        <animate attributeName="stop-color"
          values="#F9E2AF;#FF6B9D;#C77DFF;#74C7EC;#A6E3A1;#F9E2AF"
          dur="8s" repeatCount="indefinite"/>
      </stop>
      <stop offset="100%"  stop-color="#89B4FA">
        <animate attributeName="stop-color"
          values="#89B4FA;#CBA6F7;#F38BA8;#FAB387;#94E2D5;#89B4FA"
          dur="8s" repeatCount="indefinite"/>
      </stop>
    </linearGradient>

    <!-- ── AURORA RADIAL — for mark center ───────────────────────────── -->
    <radialGradient id="gr-aurora-radial" cx="40%" cy="35%" r="70%">
      <stop offset="0%"   stop-color="#CBA6F7" stop-opacity="0.95">
        <animate attributeName="stop-color"
          values="#CBA6F7;#89B4FA;#89DCEB;#CBA6F7" dur="6s" repeatCount="indefinite"/>
      </stop>
      <stop offset="40%"  stop-color="#89B4FA" stop-opacity="0.85">
        <animate attributeName="stop-color"
          values="#89B4FA;#89DCEB;#CBA6F7;#89B4FA" dur="6s" repeatCount="indefinite"/>
      </stop>
      <stop offset="100%" stop-color="#1E1E2E" stop-opacity="1"/>
    </radialGradient>

    <!-- ── NEBULA — deep space purple-blue ───────────────────────────── -->
    <linearGradient id="gr-nebula" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#8B5CF6"/>
      <stop offset="30%"  stop-color="#3B82F6"/>
      <stop offset="60%"  stop-color="#06B6D4"/>
      <stop offset="100%" stop-color="#10B981"/>
    </linearGradient>

    <!-- ── PRISM — rainbow for wave lines ────────────────────────────── -->
    <linearGradient id="gr-prism-1" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#FF6B9D" stop-opacity="0.9"/>
      <stop offset="33%"  stop-color="#C77DFF" stop-opacity="0.95"/>
      <stop offset="66%"  stop-color="#74C7EC" stop-opacity="0.9"/>
      <stop offset="100%" stop-color="#A6E3A1" stop-opacity="0.8"/>
    </linearGradient>

    <linearGradient id="gr-prism-2" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#F9E2AF" stop-opacity="0.8"/>
      <stop offset="50%"  stop-color="#FAB387" stop-opacity="0.9"/>
      <stop offset="100%" stop-color="#F38BA8" stop-opacity="0.7"/>
    </linearGradient>

    <linearGradient id="gr-prism-3" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#89DCEB" stop-opacity="0.7"/>
      <stop offset="50%"  stop-color="#94E2D5" stop-opacity="0.8"/>
      <stop offset="100%" stop-color="#A6E3A1" stop-opacity="0.6"/>
    </linearGradient>

    <!-- ── COSMIC — for text ─────────────────────────────────────────── -->
    <linearGradient id="gr-cosmic-text" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#FFFFFF"/>
      <stop offset="30%"  stop-color="#E2E8F0"/>
      <stop offset="70%"  stop-color="#CBD5E1"/>
      <stop offset="100%" stop-color="#94A3B8"/>
    </linearGradient>

    <!-- ── FIRE — accent gradient ────────────────────────────────────── -->
    <linearGradient id="gr-fire" x1="0%" y1="100%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#FF6B35"/>
      <stop offset="50%"  stop-color="#FF9E2C"/>
      <stop offset="100%" stop-color="#FFD166"/>
    </linearGradient>

    <!-- ── ICE — cool accent ─────────────────────────────────────────── -->
    <linearGradient id="gr-ice" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#A8DADC"/>
      <stop offset="100%" stop-color="#457B9D"/>
    </linearGradient>

    <!-- ── SHIMMER overlay ───────────────────────────────────────────── -->
    <linearGradient id="gr-shimmer-grad" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#FFFFFF" stop-opacity="0"/>
      <stop offset="45%"  stop-color="#FFFFFF" stop-opacity="0.12"/>
      <stop offset="50%"  stop-color="#FFFFFF" stop-opacity="0.4"/>
      <stop offset="55%"  stop-color="#FFFFFF" stop-opacity="0.12"/>
      <stop offset="100%" stop-color="#FFFFFF" stop-opacity="0"/>
    </linearGradient>

    <!-- ── HALO ──────────────────────────────────────────────────────── -->
    <radialGradient id="gr-halo" cx="50%" cy="50%" r="50%">
      <stop offset="0%"   stop-color="#CBA6F7" stop-opacity="0.35"/>
      <stop offset="40%"  stop-color="#89B4FA" stop-opacity="0.15"/>
      <stop offset="100%" stop-color="#89B4FA" stop-opacity="0"/>
    </radialGradient>

    <!-- ── INNER GLOW ────────────────────────────────────────────────── -->
    <radialGradient id="gr-inner-glow" cx="50%" cy="30%" r="60%">
      <stop offset="0%"   stop-color="#FFFFFF" stop-opacity="0.15"/>
      <stop offset="100%" stop-color="#FFFFFF" stop-opacity="0"/>
    </radialGradient>

    <!-- ── BADGE BG ──────────────────────────────────────────────────── -->
    <linearGradient id="gr-badge" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#8B5CF6" stop-opacity="0.3"/>
      <stop offset="100%" stop-color="#3B82F6" stop-opacity="0.3"/>
    </linearGradient>

    <!-- ══════════════════════════════════════════════════════════════════
         FILTERS
         ══════════════════════════════════════════════════════════════════ -->

    <!-- Outer glow -->
    <filter id="gr-f-outer-glow" x="-40%" y="-40%" width="180%" height="180%">
      <feGaussianBlur stdDeviation="8" result="blur1"/>
      <feGaussianBlur stdDeviation="16" result="blur2"/>
      <feFlood flood-color="#8B5CF6" flood-opacity="0.4" result="c1"/>
      <feFlood flood-color="#3B82F6" flood-opacity="0.3" result="c2"/>
      <feComposite in="c1" in2="blur1" operator="in" result="g1"/>
      <feComposite in="c2" in2="blur2" operator="in" result="g2"/>
      <feMerge>
        <feMergeNode in="g2"/>
        <feMergeNode in="g1"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <!-- Wave glow -->
    <filter id="gr-f-wave-glow" x="-10%" y="-80%" width="120%" height="260%">
      <feGaussianBlur stdDeviation="3.5" result="blur"/>
      <feComponentTransfer result="bright">
        <feFuncA type="linear" slope="0.6"/>
      </feComponentTransfer>
      <feMerge>
        <feMergeNode in="bright"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <!-- Letter glow -->
    <filter id="gr-f-letter" x="-30%" y="-30%" width="160%" height="160%">
      <feGaussianBlur stdDeviation="5" result="blur1"/>
      <feGaussianBlur stdDeviation="12" result="blur2"/>
      <feComponentTransfer in="blur2" result="dim">
        <feFuncA type="linear" slope="0.5"/>
      </feComponentTransfer>
      <feMerge>
        <feMergeNode in="dim"/>
        <feMergeNode in="blur1"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <!-- Drop shadow -->
    <filter id="gr-f-shadow" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="0" dy="8" stdDeviation="12"
        flood-color="#000000" flood-opacity="0.5"/>
    </filter>

    <!-- Bloom -->
    <filter id="gr-f-bloom" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur stdDeviation="8" result="blur"/>
      <feComposite in="SourceGraphic" in2="blur" operator="over"/>
    </filter>

    <!-- ══════════════════════════════════════════════════════════════════
         CLIP PATHS & MASKS
         ══════════════════════════════════════════════════════════════════ -->
    <clipPath id="gr-clip-mark">
      <circle cx="90" cy="90" r="66"/>
    </clipPath>

    <clipPath id="gr-clip-shimmer">
      <circle cx="90" cy="90" r="65"/>
    </clipPath>

    <mask id="gr-mask-fade-lr">
      <rect x="24" y="24" width="132" height="132" fill="white"/>
      <rect x="24"  y="24" width="18" height="132"
        fill="url(#gr-fade-left)"/>
      <rect x="138" y="24" width="18" height="132"
        fill="url(#gr-fade-right)"/>
    </mask>

    <linearGradient id="gr-fade-left" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="black"/>
      <stop offset="100%" stop-color="white"/>
    </linearGradient>
    <linearGradient id="gr-fade-right" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="white"/>
      <stop offset="100%" stop-color="black"/>
    </linearGradient>

    <!-- ══════════════════════════════════════════════════════════════════
         REUSABLE SYMBOLS
         ══════════════════════════════════════════════════════════════════ -->

    <!-- Particle dot symbol -->
    <symbol id="gr-sym-dot" viewBox="0 0 8 8">
      <circle cx="4" cy="4" r="4" fill="currentColor"/>
    </symbol>

    <!-- Star symbol -->
    <symbol id="gr-sym-star" viewBox="0 0 12 12">
      <path d="M6 0 L7.5 4.5 L12 4.5 L8.5 7.5 L9.5 12 L6 9.5 L2.5 12 L3.5 7.5 L0 4.5 L4.5 4.5 Z"
        fill="currentColor"/>
    </symbol>

    <!-- Diamond symbol -->
    <symbol id="gr-sym-diamond" viewBox="0 0 10 10">
      <path d="M5 0 L10 5 L5 10 L0 5 Z" fill="currentColor"/>
    </symbol>

  </defs>

  <!-- ══════════════════════════════════════════════════════════════════════
       LAYER 0 — AMBIENT BACKGROUND GLOW
       ══════════════════════════════════════════════════════════════════════ -->
  <g id="gr-bg-glow" opacity="0.5">
    <ellipse cx="90" cy="90" rx="95" ry="95"
      fill="url(#gr-halo)">
      <animate attributeName="rx" values="95;108;95" dur="5s" repeatCount="indefinite"/>
      <animate attributeName="ry" values="95;108;95" dur="5s" repeatCount="indefinite"/>
    </ellipse>
  </g>

  <!-- ══════════════════════════════════════════════════════════════════════
       LAYER 1 — MARK
       ══════════════════════════════════════════════════════════════════════ -->
  <g id="gr-mark" class="gr-mark-float" filter="url(#gr-f-shadow)">

    <!-- Outermost ring — slow aurora spin -->
    <g class="gr-mark-spin">
      <circle cx="90" cy="90" r="82"
        fill="none"
        stroke="url(#gr-aurora)"
        stroke-width="1.5"
        stroke-dasharray="6 4"
        opacity="0.4"/>
    </g>

    <!-- Second ring — counter spin -->
    <g class="gr-mark-spin-rv">
      <circle cx="90" cy="90" r="74"
        fill="none"
        stroke="url(#gr-nebula)"
        stroke-width="1"
        stroke-dasharray="3 8"
        opacity="0.5"/>
    </g>

    <!-- Main circle background -->
    <circle cx="90" cy="90" r="66"
      fill="url(#gr-aurora-radial)"
      stroke="url(#gr-aurora)"
      stroke-width="2.5"/>

    <!-- Inner glow highlight -->
    <ellipse cx="90" cy="62" rx="50" ry="30"
      fill="url(#gr-inner-glow)"/>

    <!-- ── WAVE LINES INSIDE MARK ────────────────────────────────── -->
    <g clip-path="url(#gr-clip-mark)" mask="url(#gr-mask-fade-lr)">

      <!-- Wave D — deepest, slowest, teal -->
      <path class="gr-wave-d"
        d="M 24 100 C 42 80, 62 120, 82 100 C 102 80, 118 120, 138 100
                   C 148 86, 156 106, 158 96"
        fill="none" stroke="url(#gr-prism-3)" stroke-width="2"
        stroke-linecap="round" opacity="0.3"/>

      <!-- Wave C — peach/orange -->
      <path class="gr-wave-c"
        d="M 24 92 C 42 70, 64 112, 84 92 C 104 70, 120 112, 140 92
                  C 150 76, 158 98, 158 88"
        fill="none" stroke="url(#gr-prism-2)" stroke-width="3"
        stroke-linecap="round" opacity="0.45"/>

      <!-- Wave B — purple-blue -->
      <path class="gr-wave-b"
        d="M 24 84 C 44 62, 66 104, 86 84 C 106 62, 122 104, 142 84
                  C 152 68, 158 90, 158 80"
        fill="none" stroke="url(#gr-nebula)" stroke-width="3.5"
        stroke-linecap="round" opacity="0.6"
        filter="url(#gr-f-wave-glow)"/>

      <!-- Wave A — primary rainbow, brightest -->
      <path class="gr-wave-a"
        d="M 24 76 C 44 54, 68 96, 90 76 C 112 54, 132 96, 152 76
                  C 158 62, 162 82, 158 72"
        fill="none" stroke="url(#gr-prism-1)" stroke-width="5"
        stroke-linecap="round"
        filter="url(#gr-f-wave-glow)"/>

      <!-- Fill gradient beneath primary wave -->
      <path
        d="M 24 76 C 44 54, 68 96, 90 76 C 112 54, 132 96, 152 76
                  C 158 62, 162 82, 158 72 L 158 156 L 24 156 Z"
        fill="url(#gr-aurora)" opacity="0.08"/>

      <!-- Shimmer sweeps -->
      <rect class="gr-shimmer" x="24" y="24" width="40" height="132"
        fill="url(#gr-shimmer-grad)" opacity="0.5"
        clip-path="url(#gr-clip-shimmer)"/>
      <rect class="gr-shimmer-2" x="24" y="24" width="25" height="132"
        fill="url(#gr-shimmer-grad)" opacity="0.3"
        clip-path="url(#gr-clip-shimmer)"/>

      <!-- Crestal glow nodes on primary wave -->
      <g filter="url(#gr-f-bloom)">
        <circle cx="54"  cy="76" r="4" fill="#FF6B9D" opacity="0.8">
          <animate attributeName="opacity" values="0.8;1;0.8" dur="2.1s" repeatCount="indefinite"/>
          <animate attributeName="r"       values="4;5.5;4"   dur="2.1s" repeatCount="indefinite"/>
        </circle>
        <circle cx="90"  cy="68" r="4" fill="#C77DFF" opacity="0.8">
          <animate attributeName="opacity" values="0.8;1;0.8" dur="2.7s" repeatCount="indefinite"/>
          <animate attributeName="r"       values="4;5.5;4"   dur="2.7s" repeatCount="indefinite"/>
        </circle>
        <circle cx="126" cy="76" r="4" fill="#74C7EC" opacity="0.8">
          <animate attributeName="opacity" values="0.8;1;0.8" dur="3.3s" repeatCount="indefinite"/>
          <animate attributeName="r"       values="4;5.5;4"   dur="3.3s" repeatCount="indefinite"/>
        </circle>
      </g>

    </g>

    <!-- ── CENTER LETTER ─────────────────────────────────────────── -->
    <!-- Deep glow -->
    <text x="90" y="106"
      font-family="'SF Pro Display','Inter','Helvetica Neue',system-ui,sans-serif"
      font-size="64" font-weight="900" text-anchor="middle"
      fill="url(#gr-aurora)" opacity="0.4"
      filter="url(#gr-f-bloom)">A</text>
    <!-- Main letter -->
    <text x="90" y="106"
      font-family="'SF Pro Display','Inter','Helvetica Neue',system-ui,sans-serif"
      font-size="64" font-weight="900" text-anchor="middle"
      fill="url(#gr-aurora)"
      filter="url(#gr-f-letter)"
      class="gr-letter gr-letter-a">A</text>
    <!-- Highlight stroke -->
    <text x="90" y="106"
      font-family="'SF Pro Display','Inter','Helvetica Neue',system-ui,sans-serif"
      font-size="64" font-weight="900" text-anchor="middle"
      fill="none" stroke="#FFFFFF" stroke-width="0.5"
      opacity="0.2">A</text>

    <!-- ── ORBITAL CONSTELLATION ─────────────────────────────────── -->
    <!-- Orbit path 1 -->
    <ellipse cx="90" cy="90" rx="72" ry="28"
      fill="none" stroke="url(#gr-aurora)" stroke-width="0.5"
      stroke-dasharray="2 6" opacity="0.2"
      transform="rotate(-30 90 90)"/>

    <!-- Orbital dot 1 — blue -->
    <circle r="4" fill="#89B4FA" filter="url(#gr-f-wave-glow)" opacity="0.9">
      <animateTransform attributeName="transform" type="rotate"
        from="0 90 90" to="360 90 90" dur="7s" repeatCount="indefinite"/>
      <animate attributeName="cx" values="162;90;18;90;162" dur="7s" repeatCount="indefinite"/>
      <animate attributeName="cy" values="90;62;90;118;90"  dur="7s" repeatCount="indefinite"/>
    </circle>

    <!-- Orbital dot 2 — pink -->
    <circle r="3" fill="#FF6B9D" filter="url(#gr-f-wave-glow)" opacity="0.8">
      <animateTransform attributeName="transform" type="rotate"
        from="120 90 90" to="480 90 90" dur="10s" repeatCount="indefinite"/>
    </circle>

    <!-- Orbital dot 3 — green -->
    <circle r="2.5" fill="#A6E3A1" filter="url(#gr-f-wave-glow)" opacity="0.7">
      <animateTransform attributeName="transform" type="rotate"
        from="240 90 90" to="-120 90 90" dur="14s" repeatCount="indefinite"/>
    </circle>

    <!-- Outer pulse ring -->
    <circle class="gr-mark-pulse" cx="90" cy="90" r="66"
      fill="none"
      stroke="url(#gr-aurora)"
      stroke-width="2"
      opacity="0.5"/>

  </g>

  <!-- ══════════════════════════════════════════════════════════════════════
       LAYER 2 — PARTICLE SYSTEM
       ══════════════════════════════════════════════════════════════════════ -->
  <g id="gr-particles">

    <!-- Particle specs: color · cx · cy · r · animation-class · delay · custom-vars -->
    <circle class="gr-p1" cx="168" cy="48"  r="3.5" fill="#FF6B9D" opacity="0.8"
      style="--px:6px;--py:-18px;--px2:10px;--py2:-32px"/>
    <circle class="gr-p2" cx="174" cy="82"  r="2.5" fill="#C77DFF" opacity="0.7"
      style="--px:-4px;--py:-14px;--px2:-8px;--py2:-28px"/>
    <circle class="gr-p3" cx="166" cy="120" r="3"   fill="#74C7EC" opacity="0.8"
      style="--px:8px;--py:-22px;--px2:14px;--py2:-38px"/>
    <circle class="gr-p4" cx="172" cy="60"  r="2"   fill="#A6E3A1" opacity="0.7"
      style="--px:-6px;--py:-16px;--px2:-12px;--py2:-30px"/>
    <circle class="gr-p5" cx="170" cy="104" r="4"   fill="#F9E2AF" opacity="0.6"
      style="--px:10px;--py:-20px;--px2:16px;--py2:-36px"/>
    <circle class="gr-p6" cx="164" cy="138" r="2.5" fill="#FAB387" opacity="0.7"
      style="--px:-8px;--py:-18px;--px2:-14px;--py2:-32px"/>
    <circle class="gr-p7" cx="176" cy="72"  r="3"   fill="#89DCEB" opacity="0.8"
      style="--px:4px;--py:-24px;--px2:8px;--py2:-40px"/>
    <circle class="gr-p8" cx="162" cy="94"  r="2"   fill="#EBA0AC" opacity="0.6"
      style="--px:-10px;--py:-12px;--px2:-18px;--py2:-26px"/>

    <!-- Star particles -->
    <use href="#gr-sym-star" x="169" y="35" width="8" height="8"
      fill="#F9E2AF" opacity="0.5">
      <animateTransform attributeName="transform" type="rotate"
        from="0 173 39" to="360 173 39" dur="4s" repeatCount="indefinite"/>
      <animate attributeName="opacity" values="0.5;1;0.5" dur="4s" repeatCount="indefinite"/>
    </use>

    <use href="#gr-sym-diamond" x="160" y="148" width="7" height="7"
      fill="#CBA6F7" opacity="0.4">
      <animate attributeName="opacity" values="0.4;0.9;0.4" dur="3s" repeatCount="indefinite"/>
    </use>

  </g>

  <!-- ══════════════════════════════════════════════════════════════════════
       LAYER 3 — WORDMARK
       ══════════════════════════════════════════════════════════════════════ -->
  <g id="gr-wordmark" transform="translate(196, 0)">

    <!-- ── LETTERS: ASH ──────────────────────────────────────────── -->

    <!-- Letter A — gradient + glow -->
    <g class="gr-letter gr-letter-a">
      <text x="0" y="106"
        font-family="'SF Pro Display','Inter','Helvetica Neue',system-ui,sans-serif"
        font-size="84" font-weight="900" letter-spacing="-4"
        fill="url(#gr-cosmic-text)"
        filter="url(#gr-f-letter)">A</text>
    </g>

    <!-- Letter S — aurora colored -->
    <g class="gr-letter gr-letter-s">
      <text x="60" y="106"
        font-family="'SF Pro Display','Inter','Helvetica Neue',system-ui,sans-serif"
        font-size="84" font-weight="900" letter-spacing="-4"
        fill="url(#gr-aurora)"
        filter="url(#gr-f-outer-glow)">S</text>
    </g>

    <!-- Letter H — gradient + glow -->
    <g class="gr-letter gr-letter-h">
      <text x="118" y="106"
        font-family="'SF Pro Display','Inter','Helvetica Neue',system-ui,sans-serif"
        font-size="84" font-weight="900" letter-spacing="-4"
        fill="url(#gr-cosmic-text)"
        filter="url(#gr-f-letter)">H</text>
    </g>

    <!-- ── ANIMATED UNDERLINE ────────────────────────────────────── -->
    <rect x="0" y="112" width="178" height="2.5" rx="1.25"
      fill="url(#gr-aurora)" opacity="0.7">
      <animate attributeName="width" values="0;178;178" dur="1.2s" fill="freeze"
        calcMode="spline" keySplines="0.4 0 0.2 1"/>
    </rect>

    <!-- ── VERTICAL SEPARATOR ────────────────────────────────────── -->
    <line x1="188" y1="20" x2="188" y2="130"
      stroke="url(#gr-aurora)" stroke-width="2"
      stroke-linecap="round" opacity="0.4"/>

    <!-- ── RIGHT PANEL ────────────────────────────────────────────── -->
    <g transform="translate(200, 0)">

      <!-- DOTFILES label -->
      <text x="0" y="50"
        font-family="'SF Mono','JetBrains Mono','Fira Code',monospace"
        font-size="12" font-weight="700" letter-spacing="5"
        fill="url(#gr-aurora)" class="gr-tagline">DOTFILES</text>

      <!-- Version pill — gradient border -->
      <rect x="0" y="58" width="116" height="22" rx="11"
        fill="url(#gr-badge)"
        stroke="url(#gr-aurora)" stroke-width="1.5"/>
      <text x="58" y="73.5"
        font-family="'SF Mono','JetBrains Mono',monospace"
        font-size="9.5" font-weight="800" letter-spacing="2"
        fill="url(#gr-aurora)" text-anchor="middle">v5.0 OMEGA ⚡</text>

      <!-- Shimmer on pill -->
      <rect class="gr-shimmer" x="0" y="58" width="20" height="22" rx="11"
        fill="url(#gr-shimmer-grad)" opacity="0.6"
        clip-path="url(#gr-clip-shimmer)"/>

      <!-- ── SPECTRUM SWATCHES ──────────────────────────────────── -->
      <g transform="translate(0, 92)">

        <!-- Swatch cells with gradient fills and animated pulses -->
        <g>
          <rect x="0"   y="0" width="16" height="16" rx="4" fill="#FF6B9D"/>
          <rect x="19"  y="0" width="16" height="16" rx="4" fill="#C77DFF"/>
          <rect x="38"  y="0" width="16" height="16" rx="4" fill="#74C7EC"/>
          <rect x="57"  y="0" width="16" height="16" rx="4" fill="#A6E3A1"/>
          <rect x="76"  y="0" width="16" height="16" rx="4" fill="#F9E2AF"/>
          <rect x="95"  y="0" width="16" height="16" rx="4" fill="#FAB387"/>
          <rect x="114" y="0" width="16" height="16" rx="4" fill="#89DCEB"/>

          <!-- Animated gradient overlay on all swatches -->
          <rect x="0" y="0" width="130" height="16" rx="4"
            fill="url(#gr-aurora)" opacity="0">
            <animate attributeName="opacity"
              values="0;0.3;0;0.3;0" dur="4s" repeatCount="indefinite"/>
          </rect>
        </g>

        <!-- Swatch labels (tooltips in accessible SVG viewers) -->
        <title>Catppuccin spectrum: Pink · Mauve · Sapphire · Green · Yellow · Peach · Sky</title>

      </g>

      <!-- ── FEATURE TAGS ───────────────────────────────────────── -->
      <g transform="translate(0, 120)">

        <!-- Tag 1: Themes -->
        <rect x="0"  y="0" width="56" height="16" rx="8"
          fill="url(#gr-badge)" stroke="url(#gr-prism-1)" stroke-width="0.75"/>
        <text x="28" y="11.5"
          font-family="'SF Mono',monospace" font-size="7" font-weight="600"
          letter-spacing="0.5" fill="#FF6B9D" text-anchor="middle">250+ THEMES</text>

        <!-- Tag 2: Plugins -->
        <rect x="60" y="0" width="62" height="16" rx="8"
          fill="url(#gr-badge)" stroke="url(#gr-nebula)" stroke-width="0.75"/>
        <text x="91" y="11.5"
          font-family="'SF Mono',monospace" font-size="7" font-weight="600"
          letter-spacing="0.5" fill="#74C7EC" text-anchor="middle">150+ PLUGINS</text>

      </g>

      <!-- ── MICRO STAT LINE ────────────────────────────────────── -->
      <text x="0" y="152"
        font-family="'SF Mono',monospace"
        font-size="8" font-weight="400" letter-spacing="1.5"
        fill="url(#gr-aurora)" opacity="0.6">
        AI-NATIVE · HYPRLAND · WAYLAND
      </text>

    </g>
  </g>

</svg>