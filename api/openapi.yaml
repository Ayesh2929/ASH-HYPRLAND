openapi: 3.0.3
info:
  title: ASH Dotfiles API
  version: 5.0.0-omega
  description: Generated from api/server.py — see /api/openapi.json live.
servers:
  - url: http://localhost:8787/api/v1
paths:
  /themes:
    get:
      summary: List themes
  /plugins:
    get:
      summary: List plugins
